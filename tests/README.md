# Test guide

Run commands from the project root with Godot available on `PATH`. Use an existing local `TMPDIR` when temporary storage is limited; `dist/test-work` is ignored build storage. The maintained runners create isolated project copies and private save/user-data folders. Read each runner's options before using a fixture directly.

| Area | Runner | Scope |
| --- | --- | --- |
| Construction, stairs and household ownership | `python tests/run_regressions.py --group architecture --group build --group protection` | Controlled state, routing and transaction checks; [groups and fresh-process cases](REGRESSIONS.md). |
| Short leisure before school/work | `python3 tests/run_prework_recovery.py` | Fifty-eight focused preference, urgency, Bookworm, availability, paid-recovery and departure-deadline checks (a 09:05 late riser relaxes and still arrives on time; a history-laden 08:40 break picks the mat over a canvas); the departure-deadline exception stands: a critically bored Lifelet (Fun under 12) takes the shortest pastime and accepts a few late minutes (probe precedent: 15-minute late arrival accepted at Fun 8); [scope and actual-day tradeoffs](../docs/REVIEWS/iteration_51_brief_leisure.md). |
| Household step-aside movement | `python tests/run_courtesy.py` | Genuine crowded-meal continuation, fresh restart, ownership and exclusion controls; [contract](COURTESY.md). |
| Clearing stairs and walking past household members | `python tests/run_courtesy_beneficiaries.py` | Protected stair exits, explicit floor walks, real retreat/hold saves, cancellation and compatibility; [contract](COURTESY_BENEFICIARIES.md). Use `--group stair` or `--group walk` for a focused run. |
| Passing before taking the stairs | `python tests/run_current_floor.py` | Genuine furnishing-return passage and original upstairs arrival, actual retreat/hold saves and fresh loads, FIFO validation and Build edits; [contract](CURRENT_FLOOR_CONTRACT.md). |
| Sleeping after a paused save loads | `python tests/run_rest_pose.py` | Actual selected and nonselected sleepers, a public selection/save followed by a fresh process, mattress contact, closed eyes and frozen clocks; [scope and retained failures](../docs/REVIEWS/iteration_42_paused_rest.md). |
| Shared furnishing queues | `python tests/run_busy_resources.py` | Genuine occupied-bed fixture, two-waiter order, restart, explicit interruption and Build reconciliation; [contract](BUSY_RESOURCES.md). |
| Occupied activities and repeated detours | `python tests/run_activity_flow.py` | Genuine occupied-easel recovery, admission and autonomy controls, bounded retries, Pause/Build, and an actual save followed by a fresh process; [scope and retained failures](../docs/REVIEWS/iteration_43_activity_flow.md). |
| Owned meal approaches | `python tests/run_owned_meals.py` | Walking with the same partial portion, fresh restart, meal-service and autonomy controls; [contract](OWNED_MEALS.md). |
| Home sidewalk walkers | `python tests/run_pedestrian_checks.py` | Nine controlled lane, obstacle, old-position, re-entry and home-visit transfer cases. |
| Public two-storey flow | `python tests/run_public_twofloor.py` | Creator, construction clicks, upstairs activity, named restart and car travel. |
| Sanitation across floors | `python tests/run_sanitation_levels.py` | Wet-patch support, stair custody, named restart and car travel. Add `--capture` for separate rendered picking/paused-mop checks; `--only v2` or `--only car` narrows the run. See [the contract](../docs/SANITATION_API.md). |
| Social conversation routing | `python tests/run_social_navigation.py` | Body-clear neighbor/household approaches, moving targets, protected stairs, later queues and three paused saves resumed in a fresh process. Controlled fixtures; [scope and limits](../docs/REVIEWS/iteration_31_social_routes.md). |
| Household and neighborhood | `python tests/run_playthrough.py --suite home` or `--suite neighborhood` | Rendered directed play and fresh-process persistence. |
| Recipes and crowded dining | `python tests/run_playthrough.py --suite recipes` or `--suite meal_placement` | Meal selection, serving, leftovers, furniture edits and paid-action restart. |
| Meals with a welcomed guest | `python tests/run_guest_meals.py` | Actual meal claim, shared company, chair/standing custody, Goodbye retry, malformed-save admission and fresh-process restoration. Add `--capture` for separate Forward+ paused poses. [Review and evidence limits](../docs/REVIEWS/iteration_28_guest_meals.md). |
| Oven preparation | `python tests/run_oven_playthrough.py` | Loading, closed baking, two paused fresh-process saves, one ingredient charge and later queued reading. |
| Adoption | `python tests/run_playthrough.py --suite adoption --adoption-oven --timeout 300` | Public adoption during paid cooking, mid-arrival restart, school and homework. Add `--adoption-ui-only` for the focused 960×600 candidate/review/cancel flow. |
| Save previews | `python tests/run_save_preview.py --capture` | Same-frame panel cancellation, direct save, public save-as-new, overwrite and deletion. Omit `--capture` for headless state checks; image freshness requires the rendered run. |
| Second-collection week | `python tests/run_playthrough.py --suite catalogue_week --timeout 2400` | Rendered seven-day audit of the eight-member Reed family with a treadmill, toy chest, piano, bathtub, stereo, yoga mat, mirror and armchair bought through the public purchase path, one Active adult, and a 5×3.5 m garden annex with a west doorway built through the public build transactions holding a second bed, fridge and desk (`JUSTLIFE_WEEK_ANNEX=0` keeps the advertised one-bed cottage for comparison); asserts that each purchased activity furnishing is used at least once (four weeks of audits show per-action rotation with Fun floors 23-46, so the former three-use bar was re-scoped — see the check comment), that a non-Active adult varies leisure, that weekly waiting stays at or under four hours per member, that Fun never drops below 14, that no need enters the critical zone and that nobody arrives late at school or work,|
| Queue-aware resources | `godot --headless --path . --audio-driver Dummy --script res://tests/test_queue_choice.gd` | Headless: an occupied fridge with a waiter counts its queue, so a hungry Lifelet takes a free second fridge instead; a sofa with a user and two waiters reads as loaded for the pre-duty fitting check; free resources keep the established preference. |
| Plates on a table | `godot --headless --path . --audio-driver Dummy --script res://tests/test_plate_reach.gd` | Headless: a furnishing's collision box is taller than its own tabletop, so a plate set on a table sat inside the table's box and was unreachable; food and spills are now picked on their own surface bit ahead of the furniture ray. Real dispatched mouse presses open the plate's own menu (wash, put away, bin) and the table's Clear the table, which really gathers and washes the plates. |
| The doorbell saves | `godot --headless --path . --audio-driver Dummy --script res://tests/test_doorstep_save.gd` | Headless: a neighbour reaches the front path and rings through the production entry point; the live doorstep is a `Vector3`, the snapshot packs it as an `Array`, and the packed record passes the save validator and restores the caller to the same ground point. The household snapshot also carries a ringing doorbell that has no active visit yet, so saving while someone waits at the door no longer drops them. |
| The doorbell | `godot --headless --path . --audio-driver Dummy --script res://tests/test_doorbell.gd` | Headless: a caller on the front path rings the bell and waits outside instead of wandering in; turning them away leaves them outside and they cannot re-ring the same day; letting them in starts an ordinary invited visit and walks them inside; asking them to leave works while they are inside. |
| A pet's own actions, and the baby bump | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_touch_and_bump.gd` | Headless: the pet card's own **Give a tummy rub** button really queues and completes through the public path without crashing (a pet item carries no `node`, so `queue_interaction` used to read one through `world.approach` and raise *Invalid access to property or key 'node'*), and an expectant mother's `PregnancyBump` really measures as a body-sized belly that grows with the term — 0.29 m mid-term to 0.37 m near birth — rather than the ~2 m sphere the authored ellipsoid was overwritten into. |
| Clicking a pet | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_click.gd` | Headless: the real phone flow buys a pet, and clicking its body dispatches to that pet rather than to a walk on the ground, opening its own card. |
| Moving garden furnishings | `godot --headless --path . --audio-driver Dummy --script res://tests/test_garden_move.gd` | Headless: reads the garden-voucher families straight from the shipped catalogue (`Garden`, `Pool`, `Kids`, `Vehicles`) and, for each that the lot has room for, buys it through the ordinary Build & buy path and then really moves it. Two defects crashed the game here: `move_item` asked `begin_placement` for the bare kind, but a family whose art is styled (shrubs, flowers, fences, hot tubs, pools, tree) ships no base model, so `load(path).instantiate()` ran on a null resource; and `on_placement` read `ITEMS[kind].price`, which a fence does not have because it sells by the square metre. **12 families bought, 12 moved, 0 errors**; reverting either fix raises 27 script errors. |
| The garden and the dog house | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_garden.gd` | Headless: the enlarged lot is walkable to its rim in both the graph and the compatibility grid, and a kennel and a garden bed are bought through the public purchase path and stand in the garden, then pass home-layout validation. |
| Pet beds, toys and toy boxes | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_accessories.gd` | Headless: the Cat Toy Box and Dog Toy Box cost ℒ50 each and are named as such, and a collar and leash colour chosen in the real phone picker is stored on the pet and tints both authored accessory surfaces. |
| Progression (criminal/technical careers, insurance, skill ceiling) | `godot --headless --path . --audio-driver Dummy --script res://tests/test_progression.gd` | Headless: the Criminal track pays exactly ℒ1000 for one worked shift through the ordinary queue and lands it on the shared household funds; Technical is refused below Logic 8 with a reason naming the level and accepted at 8, charging its ℒ900 course fee once; a burglar really takes ℒ600 on its scheduled night (and nothing off-schedule) while home insurance buys once, refuses a repeat with a reason, reimburses an insured break-in in full, survives a real save and fresh household restore, and has an unknown policy rejected; and every `LifeSim.SKILL_NAMES` entry has a book, studying at level 9 is refused with a reason, and the computer carries a subject to exactly level 10 (refused once mastered). Also keeps the original suite's standalone mood/memory persistence, career switching, promotion and snapshot round trip. |
| Rotate and the camera | `godot --headless --path . --audio-driver Dummy --script res://tests/test_rotate_and_camera.gd` | Headless: a real R key press turns the placement and the ghost, a real click commits it, and a move keeps its rotation at every quarter turn; a real right-drag orbits and pitches, and the wheel reaches the new zoom floor and ceiling. |
| The creator's Style tab | `godot --headless --path . --audio-driver Dummy --script res://tests/test_creator_style.gd` | Headless: ten hairstyles, a lip and eye set (full for girls, limited for boys), and jewelry anyone may wear, with 100 or more selectable clothing options across the five outfit categories. |
| Land and lot expansion | `godot --headless --path . --audio-driver Dummy --script res://tests/test_land.gd` | Headless: a neighbouring plot on a side is really bought through the Build transaction at the policy's own price, the lot grows by exactly that plot, the newly bought ground is walkable in the live graph, the back hedge moves to the new edge, a kennel fits where it would not go before and is still refused beyond the new boundary, six plots can be bought one after another, a northern plot deepens the garden without moving the street, an unaffordable purchase is refused with no land and no money taken, and the land round-trips through a real save and a fresh load while a corrupt record is refused. |
| Homes, moving and per-house insurance | `godot --headless --path . --audio-driver Dummy --script res://tests/test_properties.gd` | Headless: three starter houses are offered and the two larger ones cost more than the smaller; a household starts with exactly one home, chooses which starter house to begin in, buys the largest house and moves into it for exactly the quoted price with its own furnishings really built, carries premium cover whose payout exceeds the loss, moves back to an uninsured home that then carries no cover while the house left behind keeps its own policy, every house type gets a real panel row, and both homes and both policies round-trip through a real save and load with a save living in an unowned house refused. |
| Rooms, shared walls and extensions | `godot --headless --path . --audio-driver Dummy --script res://tests/test_room_extension.gd` | Headless: a too-small room is refused with a reason and changes nothing, an added room really raises four walls and a floor and costs its quote, a room drawn against another shares the one dividing wall instead of doubling it, breaking that wall refunds part of its cost and leaves the two rooms one open space a walker can cross, a doorway can still be cut in a surviving wall, the house can be extended further out, and an extension can carry on onto a plot bought next door — with the enlarged home still passing the world's own layout validation. |
| The town's working venues | `godot --headless --path . --audio-driver Dummy --script res://tests/test_venues.gd` | Headless: eight working venues are offered and every one is valid with a real layout and described services; the filling station really offers both petrol and electric charging; the town now has sixteen places with both a new venue and an original place resolving; a wall charger and an electric car are sold, the charger names what it charges and must be wall-mounted through its own entry's `wall_mounted` flag — which `LifeCatalog.wall_mounted(kind)` is the one authority for, so the live game refuses the charger free-standing in the open garden while accepting an ordinary furnishing on the same spot and placing the charger against a clear stretch of wall; every place other than home has a list row; the café is travelled to and really built and walkable and cannot be edited as a home; seven more venues are each travelled to and built and walkable; and a venue visit saves and resumes while an unknown venue is refused. |
| The weekly shop and the delivery van | `godot --headless --path . --audio-driver Dummy --script res://tests/test_groceries.gd` | Headless: a fresh kitchen is empty and refuses to cook; the three baskets are ordered and priced, an evening order arrives tomorrow, a second delivery cannot be ordered while one is on its way, an unaffordable shop is refused, and the kitchen runs out when everything is eaten; in the real game the fridge no longer sells a meal (the cook and snack actions are refused with a reason naming where to order, and the fridge carries the household's own order entry, so a home without a computer can still shop), the panel charges the purse exactly once, the organic van with its three-leaf sign stands outside while the order is on its way, the delivery arrives on the household's own clock and fills the kitchen, the van leaves once it is carried in, drawing a meal takes no money, and the kitchen rides the save while a corrupt one is refused. |
| Ninety days of progression | `python tests/run_playthrough.py --suite ninety_day_progress --timeout 2400` | Rendered, isolated: two adults on Willow Cottage live autonomously for **ninety game days** through the household's own clock at the fastest supported speed. The run stocks its own kitchen through the household's order, awards a Bachelors at the higher-education panel, and then proves the player really got somewhere — a skill past level 5, a career at rung 3 or better with a paid promotion on the record, a degree held, the purse never drained, and **nobody starved** (the failure this suite was written to catch: an empty kitchen with no order path killed an unattended household in four days). A fresh process then reloads the named save and confirms the career rungs, every skill and the qualification came off disk. Writes `user://ninety_day_progress.json`. |
| Businesses | `godot --headless --path . --audio-driver Dummy --script res://tests/test_business.gd` | Headless: the level-9 bar and the brief's two price points are enforced on every business and stated by the same numbers that refuse; a Lifelet one level short is refused against that bar, a low skill is refused naming the actual level, an unaffordable business names its shortfall, a child is refused, and an unknown business or a doubly-hired employee in a save is refused; more employees really raises the takings; and in the real game a poor unskilled household is refused, the panel lists every business, the chosen one is bought for exactly its price, a second is refused without replacing the first, a housemate is hired for the table's fee and raises the income, the business takes its daily income on the household clock, and the business and its employee round-trip through a real save. |
| Degrees and the Dr title | `godot --headless --path . --audio-driver Dummy --script res://tests/test_education_degrees.gd` | Headless: the qualification ladder is Bachelors, Masters and PHD, each with a real fee and duration, a higher degree needs the one below it, and only a PHD is a doctor; a degree is worth more only to the jobs that ask for one and nothing to waiting tables; the university opens its own enrolment desk; the whole ladder is walked with each fee charged once and the Lifelet really holding each qualification; the profile really shows the Dr title; the same job at the same rung pays less without the degree; and a save with a PHD loads with the doctor still a doctor while an unknown qualification is refused. |
| Prison, sentences and visits | `godot --headless --path . --audio-driver Dummy --script res://tests/test_prison.gd` | Headless: a caught Lifelet really leaves the household's lot for Blackmoor, the HUD says they are inside until a named day, they cannot take a job while serving and cannot be caught again, the prison's visiting desk opens with a family visit and meeting them at the gate, a real visit meets the visitor's social need and leaves the family member inside feeling better and remembering who came, the household's own clock reaches the release day and the Lifelet really comes home while the arrest stays on their record, a Lifelet caught while already away serves from home rather than holding two absences with their original absence intact, and an invalid sentence location is refused. |
| New panels fit the design space | `godot --headless --path . --audio-driver Dummy --script res://tests/test_panel_bounds.gd` | Headless: every control the phone (with its new insurance row), the wardrobe panel in all three tabs, the dish's invitation panel, the cookbook and the eight-track career picker build sits inside the authored 1440x900 space, so none is clipped off the edge. The dish's panel is also checked for *content*: every invited Lifelet must really get a row and a pressable offer button, because an invalid property assignment used to abort its row loop and leave an empty card. The cookbook must keep its dishes in a scroll region (absolute rows ran the last four off-canvas at ten recipes), and all four creator tabs must sit inside the canvas (a wider pitch clipped **Style**). |
| The whole interface, rendered | `godot --path <isolated project> --audio-driver Dummy --script res://tests/probe_ui_overlap.gd` | Rendered: emulates Godot's own control picking over 50 screens — menus, creator, live HUD, every household/career/progression panel, phone, calendar, pet shop and picker, all four wardrobe tabs, the dressing table, food offers, leftovers, the cookbook, item interaction menus, Build & buy categories and storage — and asserts every visible mouse-interactive control sits inside the canvas and no two overlapping siblings both accept the mouse. **50 screens, 0 overlaps, 0 escapes, 0 failures.** Needs a real display and an isolated project (its guard requires a `justlife-camera-*` path with private `XDG_DATA_HOME`/`JUSTLIFE_DATA_DIR`). |
| The wardrobe, mirror and dressing table | `godot --headless --path . --audio-driver Dummy --script res://tests/test_wardrobe_panel.gd` | Headless: clicking a wardrobe offers opening it, and finishing that beat opens the styling panel with Clothes, Hair, Makeup and Jewelry tabs. Trying a look on the real Lifelet is free and really changes what is shown (another hairstyle leaves exactly one style visible on the model); keeping it costs one look price and rides the saved look. The full-length mirror gives one whole Charisma level a day (once per game day), and the dressing table offers makeup and jewelry. |
| Doorway yielding | `godot --headless --path . --audio-driver Dummy --script res://tests/test_doorway_yield.gd` | Headless: a one-door room built on the empty lot with a fridge just inside the door; an idle Lifelet on the use point steps aside for a walker, and a walker and a strolling Lifelet meeting head-on in the doorway both get through. Two-person cases; the eight-person annex freeze of iteration 58 is covered by the rendered annex day below. |
| Social variety | `godot --headless --path . --audio-driver Dummy --script res://tests/test_social_variety.gd` | Headless: the hug needs 25 friendship for the full gain with a repeat cooldown, and sharing interests lands deeply only with shared traits (polite but small otherwise) — both record the met milestone. |
| Social route cooldown | `godot --headless --path . --audio-driver Dummy --script res://tests/test_social_cooldown.gd` | Headless: two consecutive routing failures put a neighbour on a three-hour chooser cooldown — the member falls back to the other neighbour, a fresh member still considers both, and a lapsed cooldown returns the neighbour to the pool. |
| Room-aware paint | `godot --headless --path . --audio-driver Dummy --script res://tests/test_room_paint.gd` | Headless: whole-room paint floods the enclosed floor region on the clicked side — the bedroom click repaints exactly the bedroom's walls (partition included, shared with the next room), door gaps stay boundaries, a freestanding garden wall paints alone, and the single-wall scope still exists. |
| Baby life stage | `godot --headless --path . --audio-driver Dummy --script res://tests/test_baby_stage.gd` | Headless: `baby` is registered before `child` and chains baby→child (labels, duration, minor eligibility, article, an old save without the field, and a birthday handing its school record to the child stage while still refusing to move backward); the actor loads the authored `character_baby.glb`, keeps nine joints and the shared blink/smile/identity controls, and a moving baby pitches its torso forward 1.22 rad with hips under 0.22 m above its root and limbs cycling through the stride phase, while a still baby kneels and Pause freezes both; adult-only work, study, hobbies and the treadmill are refused for a baby through `LifeSim.get_action_availability` while sleep, snack, toilet and its birthday still work. The model checks skip-with-pass and report it when the GLB is not imported. |
| Iteration-60 living room | `godot --headless --path . --audio-driver Dummy --script res://tests/test_living_v60.gd` | Headless: the storybook reading nook, teatime coffee table and reading arc floor lamp are bought, rotated, moved and sold through the public build path; the nook seats its queued reader and a Bookworm's chooser picks it over a free easel; the coffee table offers a real platter slot with the authored cup-and-book prop visible; the lamp's menu switch rides the layout record across a fresh load and a move, and both the coffee table and the lamp are purchased on a supported upper floor bought through the public structure transactions with the upstairs lamp still persisting its switched-off state. |
| Second-storey guidance | `godot --headless --path . --audio-driver Dummy --script res://tests/test_second_storey_guide.gd` | Headless: pressing Upper with no upper floor starts the Floor tool itself and quotes through the public transactions; an oversized off-centre drag is fitted to the walled span (ℒ1440) rather than refused, while a rectangle that misses the walls entirely still names the two-opposite-bearing-walls rule; the full slab commits through the public callback and the stair step lands opening and guard together with validating architecture. The rendered two-floor suite owns actual pointer clicks. |
| Household extras | `godot --headless --path . --audio-driver Dummy --script res://tests/test_household_extras.gd` | Headless: a rubbish bin fills from cooking/eating/clearing and refuses an impossible saved fill; a skill book bought for a shelf teaches its subject and is refused on a missing shelf or unknown subject; guitar and violin practise share one Music action; books and bin fill ride `household.get_state()["extras"]` and survive a real save followed by a fresh load. Also covers the storage unit (a furnishing files away, counts against the 30-item cap, is taken back out or sold, and refuses an unknown kind), the one-game-day fresh window and the `Throw it in the bin` action on spoiled food, the household's 0..1 pregnancy progress, and the wall sightline that stops conversations through a partition. |
| Music and pets | `godot --headless --path . --audio-driver Dummy --script res://tests/test_music_and_pets.gd` | Headless: Summit Dawn loads as a full-length looping theme, plays at startup and follows both switches (music alone pauses it; the sound switch silences it too; the pause menu offers its own `Music:` row); the shop policy prices cats and dogs differently and always offers both species and sexes while refusing an unnamed, badly coloured or unknown-species review; a solid and a fully mixed coat keep their two colours and gradient through `LifePets.appearance`; a real cat is prepared, charged exactly once, recognised on a repeated confirmation, spawned with a body that walks in and can be picked, then survives a real save and a fresh load with its identity, species, both colours and gradient intact while an impossible gradient or a wrong price is refused; and the accessories place through the ordinary furnishing path with a bowl allowed for any pet, a cat tree only for a cat household and a kennel only for a dog household. See [the pets guide](../docs/PETS.md). |
| Hygiene and put-away | `godot --headless --path . --audio-driver Dummy --script res://tests/test_hygiene_putaway.gd` | Headless: wash_hands and brush_teeth both raise hygiene with the brush the deeper freshen-up, a finished toilet visit queues its own wash at a reachable sink, and a plated serving left out returns to its dish in the fridge through the public fridge action (remaining count restored, plate gone). |
| Wardrobe looks and passing | `python3 tests/run_character_quality.py --tests outfit_categories passing look_garments` | Headless, isolated: five saved looks, activity auto-wear and Everyday revert, original Formal/Athletic/Sleep/Party GLBs, old-age and need deaths, inspectable keepsakes, spirit rules and a garden stone on a starter lot. |
| Character identity controls (iteration 63) | `python3 tests/run_character_quality.py --render probe_character62` | Rendered, isolated: all eleven identity controls exist on every production model and every age — the probe asserts each of the eleven on each of the six life stages in the real creator, and fails on any model that exposes fewer. Passes **5,178 checks, 0 failures**; it failed 266 times before iteration 63, when only four of the eleven controls existed on the shipped assets. Also captures six generated identity profiles, the Bob turntable and recolours, every signed control at both extremes with Blink, and one face/body frame per age into `user://character62`. See [the iteration 63 review](../docs/REVIEWS/iteration_63_identity_controls.md). |
| Pinned character hashes | `python3 tools/requalify_character_hashes.py --check` | No display or engine needed: compares all twenty `art/source/character_production_hashes.json` entries and the four `tools/verify_character_exports.py` semantic pins against the files on disk. Exits non-zero on any staleness. It exists because hand-editing those records already shipped two real defects during iteration 63 — four `.blend` entries left at pre-promotion hashes, and four pins written as raw file digests where the verifier compares semantic digests. Run the tool with no `--check` to refresh both records after a reviewed promotion; it re-reads them afterwards and fails if anything still disagrees. |
| Iteration-61 depth package | `godot --headless --path . --audio-driver Dummy --script res://tests/test_depth_v61.gd` | Headless: the spendable Rewards store pays exactly its price for permanent perks (a real 30% bladder-decay cut, a real +25% career-performance lift) and one-use potions (hunger refilled, and the Inspired mood granted for its advertised 480 minutes), refuses unaffordable and already-owned rewards with a reason and no state change, and every new field survives a real JSON round trip while a corrupted `purchased_perks` is rejected; the emotion-gated interactions are refused outside their mood and offered inside it (the Inspired masterpiece sells for its promised ℒ285 through the public interaction path, the thin-friendship prank really backfires into Tense), and each trait habit is offered only to that trait and carries a real, distinct effect. |
| Resident body gap | `godot --headless --path . --audio-driver Dummy --script res://tests/test_resident_body_gap.gd` | Headless: a visiting resident never steps closer to a parked body inside the shared gap and abandons an occupied waypoint for the next one; the bare-lot boarding walk refuses steps that would enter the gap. Walking bodies keep the traversal BODY_GAP everywhere residents move without the traversal planner. |
| Route learning | `godot --headless --path . --audio-driver Dummy --script res://tests/test_route_learning.gd` | Headless: graph edges under a walker's actually refused steps are penalized through the public navigation object, the next route detours around the learned corridor, and expired penalties restore the original shortest route — so a walker never presses into furniture on a corridor the planner should not have chosen. |
| Stuck upstairs on a stair ticket | `godot --headless --path . --audio-driver Dummy --script res://tests/test_stuck_upstairs.gd` | Headless: a ground appliance grazing a bought staircase's lower landing used to make the floor graph drop the whole staircase, so no route joined the floors and an opposing pair stood on a queue ticket while nothing moved; the staircase stays linked, an opposing public pair each really crosses it and reaches its floor, and an away Lifelet (no body in the lot) is refused a route with a reason instead of silently holding one. |
| Shared bed | `godot --headless --path . --audio-driver Dummy --script res://tests/test_shared_bed.gd` | Headless: a partnered pair queues sleep on one bed and lies on its two halves (left/right, ~1.3 m apart) through the public selection and interaction queue; a non-partner housemate is refused the occupied bed with a clear notice. |
| Couch seating | `godot --headless --path . --audio-driver Dummy --script res://tests/test_couch_seating.gd` | Headless: the Sunday sofa really seats **three** and the loveseat **two**, each place on that model's own authored cushion (the sofa's three at x=-0.70, 0 and +0.70; the loveseat's two at ±0.37) rather than a spread invented from the footprint. Three Lifelets queue through the public interaction path and hold three distinct places, their seated anchors more than 0.5 m apart; a fourth claims **no** place and is refused admission while all three cushions are taken, then takes a place the moment one frees; the loveseat holds exactly two and refuses a third. 23 checks, 0 failures. |
| Couch seating, rendered | `python tests/run_playthrough.py --suite couch` | Rendered, isolated, two processes: three creators settle a fresh household, buy a Sunday sofa through the public purchase path, and each click it and sit; the suite asserts all three hold different cushions and are seated on the one sofa at once, more than 0.5 m apart, then saves through the public picker. A fresh process loads that save and confirms the sofa still has three places with the same three Lifelets on three different cushions. **51 checks + 7 resume, 0 failures.** The capture `01_three_on_the_sofa.png` shows the three seated together. |
| The two-car garage | `godot --headless --path . --audio-driver Dummy --script res://tests/test_car_garage.gd` | Headless: the two-car garage really places through the public Build & buy path (its catalogue key `car_garage` now names the authored model it ships, which used to be missing so the button silently did nothing), and its own interior stays unblocked so a car stands inside it while its walls, posts and roof still block. |
| Try for Baby and the baby creator | `godot --headless --path . --audio-driver Dummy --script res://tests/test_make_baby.gd` | Headless: the bed menu offers Try for Baby exactly for a male+female adult partner pair asleep in one bed (the household plan names the female partner as mother), and refuses same-gender, non-partner, not-asleep, baby-already-present and full-household cases through the ordinary notice path; the chosen action runs one bounded 40-second beat with a single shared token whose clock exactly one partner owns, no charge, the interrupted sleep waiting behind it, and a clean return for both partners on cancel; completing it stores the rolled child as a pending baby that the player names in the ordinary creator (seeded as the baby stage, offering that stage alone), and confirming adds the edited name/gender/skin/hair/eyes/face baby to the household at its home lot with both parents in the family graph; a save round-trips the created baby, a save without the pregnancy field stays readable, and a malformed pregnancy record is refused. The covers-ruffle overlay is presentation only and is covered by the rendered capture. |
| Plates on a table | `godot --headless --path . --audio-driver Dummy --script res://tests/test_plate_reach.gd` | Headless: a furnishing's collision box is taller than its own tabletop, so a plate set on a table sat inside the table's box and was unreachable; food and spills are now picked on their own surface bit ahead of the furniture ray. Real dispatched mouse presses open the plate's own menu (wash, put away, bin) and the table's Clear the table, which really gathers and washes the plates. |
| The doorbell saves | `godot --headless --path . --audio-driver Dummy --script res://tests/test_doorstep_save.gd` | Headless: a neighbour reaches the front path and rings through the production entry point; the live doorstep is a `Vector3`, the snapshot packs it as an `Array`, and the packed record passes the save validator and restores the caller to the same ground point. The household snapshot also carries a ringing doorbell that has no active visit yet, so saving while someone waits at the door no longer drops them. |
| The doorbell | `godot --headless --path . --audio-driver Dummy --script res://tests/test_doorbell.gd` | Headless: a caller on the front path rings the bell and waits outside instead of wandering in; turning them away leaves them outside and they cannot re-ring the same day; letting them in starts an ordinary invited visit and walks them inside; asking them to leave works while they are inside. |
| A pet's own actions, and the baby bump | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_touch_and_bump.gd` | Headless: the pet card's own **Give a tummy rub** button really queues and completes through the public path without crashing (a pet item carries no `node`, so `queue_interaction` used to read one through `world.approach` and raise *Invalid access to property or key 'node'*), and an expectant mother's `PregnancyBump` really measures as a body-sized belly that grows with the term — 0.29 m mid-term to 0.37 m near birth — rather than the ~2 m sphere the authored ellipsoid was overwritten into. |
| Clicking a pet | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_click.gd` | Headless: the real phone flow buys a pet, and clicking its body dispatches to that pet rather than to a walk on the ground, opening its own card. |
| Moving garden furnishings | `godot --headless --path . --audio-driver Dummy --script res://tests/test_garden_move.gd` | Headless: reads the garden-voucher families straight from the shipped catalogue (`Garden`, `Pool`, `Kids`, `Vehicles`) and, for each that the lot has room for, buys it through the ordinary Build & buy path and then really moves it. Two defects crashed the game here: `move_item` asked `begin_placement` for the bare kind, but a family whose art is styled (shrubs, flowers, fences, hot tubs, pools, tree) ships no base model, so `load(path).instantiate()` ran on a null resource; and `on_placement` read `ITEMS[kind].price`, which a fence does not have because it sells by the square metre. **12 families bought, 12 moved, 0 errors**; reverting either fix raises 27 script errors. |
| The garden and the dog house | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_garden.gd` | Headless: the enlarged lot is walkable to its rim in both the graph and the compatibility grid, and a kennel and a garden bed are bought through the public purchase path and stand in the garden, then pass home-layout validation. |
| Pet beds, toys and toy boxes | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_accessories.gd` | Headless: the Cat Toy Box and Dog Toy Box cost ℒ50 each and are named as such, and a collar and leash colour chosen in the real phone picker is stored on the pet and tints both authored accessory surfaces. |
| Rotate and the camera | `godot --headless --path . --audio-driver Dummy --script res://tests/test_rotate_and_camera.gd` | Headless: a real R key press turns the placement and the ghost, a real click commits it, and a move keeps its rotation at every quarter turn; a real right-drag orbits and pitches, and the wheel reaches the new zoom floor and ceiling. |
| The creator's Style tab | `godot --headless --path . --audio-driver Dummy --script res://tests/test_creator_style.gd` | Headless: ten hairstyles, a lip and eye set (full for girls, limited for boys), and jewelry anyone may wear, with 100 or more selectable clothing options across the five outfit categories. |
| The wardrobe, mirror and dressing table | `godot --headless --path . --audio-driver Dummy --script res://tests/test_wardrobe_panel.gd` | Headless: clicking a wardrobe offers opening it, and finishing that beat opens the styling panel with Clothes, Hair, Makeup and Jewelry tabs. Trying a look on the real Lifelet is free and really changes what is shown (another hairstyle leaves exactly one style visible on the model); keeping it costs one look price and rides the saved look. The full-length mirror gives one whole Charisma level a day (once per game day), and the dressing table offers makeup and jewelry. |
| Doorway yielding | `godot --headless --path . --audio-driver Dummy --script res://tests/test_doorway_yield.gd` | Headless: a one-door room built on the empty lot with a fridge just inside the door; an idle Lifelet on the use point steps aside for a walker, and a walker and a strolling Lifelet meeting head-on in the doorway both get through. Two-person cases; the eight-person annex freeze of iteration 58 is covered by the rendered annex day below. |
| Loveseat watchers | `python tests/run_playthrough.py --suite loveseat --timeout 600` | Three adults on the empty lot buy a television, a loveseat and an armchair through the public purchase path and watch in turn: the first two take the left and right loveseat seats, the third is seated in the free armchair rather than queueing at the full loveseat. |
| Pet care, tricks and garden games | `godot --headless --path . --audio-driver Dummy --script res://tests/test_pet_care.gd` | Headless: a bought cat has its own condition (six needs, three skills, bonds, tricks); its needs fall on the household's own clock and a paused household freezes them; feeding fills it up and deepens its bond with the person who fed it; repeated trick teaching raises its Tricks skill and it really knows the tricks it earned; **a child teaching a trick also grows in Logic**, a baby is refused with a reason and training obedience is refused to a child; the whole condition survives the save and a damaged need is refused; and the fifty garden games are catalogued, modelled, gated by age, priced and playable through the ordinary queue with the game's own skill bound at queue time. 47 checks, 0 failures. |
| Styles, colours and sizes | `godot --headless --path . --audio-driver Dummy --script res://tests/test_garden_catalogue.gd` | Headless: the three variant axes and their defaults; every style a catalogue entry offers has its authored model and every colour-variant model authors the surface its colour paints; the three price shapes (a fence at its own rate per square metre, a stated price per size, one price) and the per-size seat count; a chosen style, colour and size survive the ordinary purchase, reach the model's own `Tint` surface and ride the saved layout; the picker offers every choice and fits the design space; and a large garden table really holds ten distinct places while a bed still keeps its named halves. 48 checks, 0 failures. |
| Household calendar | `python tests/run_calendar.py --capture` | Read-only school/work/birthday forecasts, eight-member phone flow, filtering, scrolling, queued-action preservation and pause restoration. Captures include a native 960×600 window. |
Rendered fixtures require a display AND an otherwise-idle machine: concurrent rendered runs or heavy GPU work stall the social timing windows (observed as intermittent "walk from shower to neighbor" timeouts; four consecutive exclusive runs pass while interleaved runs fail). Some grants set needs, disable autonomy or invoke public button callbacks directly. They exercise the stated paths, not unscripted whole-game play or a general quality score. Individual assertions are not additional gameplay scenarios.
Since the iteration 50 clock change, Normal speed advances one game minute per real second; the busy-resource, pedestrian and home-visit harness windows were widened in iteration 57 to observe the same rules at that pacing (their earlier budgets could not see a 30-minute wait, two walker cycles or a 360-minute stay).

The Linux-only activity-flow runner accepts `--source /path/to/checkout`. It combines the existing dependency manifest with its explicit test and fixture inputs, copies them into a private project, strips plugin/autoload configuration there, and isolates save, data, config, cache and temporary paths. Its six phases cover natural recovery, admission, retry components, public Pause/Build, public Save during an observed obstruction and a fresh process loading that generated save. The save pair shares the original 600 advancing-call budget: 107 before saving and at most 493 after loading. The runner compares all 107 producer rows with its natural prefix, validates fresh phase reports against terminal counts, and verifies input and pre-existing save hashes. The initial maintained extraction passed 226 assertions; these remain focused checks rather than an endurance, attendance, rendering or release gate.

The exploratory `--suite dense_meals --timeout 900` retains a known late-dinner checkpoint failure. Read [the dish-placement review](../docs/REVIEWS/iteration_14_dish_placement.md) before interpreting its results. Later repairs and their narrower evidence remain in their respective reviews. Logs, screenshots, traces and manifests are local evidence; failures and warnings must remain visible when reporting a result.

[Release verification](../docs/RELEASE_VERIFICATION.md) records actual exported-binary checks and separates them from source tests. [Independent reviews](../docs/REVIEWS/) record acceptance decisions and remaining visual or behavioral limits.
