# Packaged release verification

The current Linux build is from commit **`5048368`**, adding the accepted adult eyelid and lip refinements and their reproducible Blender sources. The actual Forward+ executable passes **58 checks with zero failures, errors or warnings** on the available NVIDIA RTX 5090, using Dummy audio. All 999 archived source files and 20 current character pins match the exact commit. Of 152 runtime inputs, only the four adult GLBs differ from `75b91c0`; the 15 nonadult files and all runtime scripts remain exact.

Executable SHA-256: `2d7e1ddbffe152fd12c3f73020bafffab78d18a90565cfe0c1631ebd2d10789e`; Linux archive SHA-256: `195de40ca9de07107ef541df47b9b7ebdbff5155018a193cd938e2d211765ba9`. The frozen handoff is `dist/test-work/justlife-package-5048368-yiuena72/handoff`. The packaging agent inspected all nine captures; root also inspected the Live household and save picker, whose thumbnail correctly shows the current household. The unchanged probe covers startup, creator/family controls, calendar, adoption cancellation, save/load/delete and the real fifteen-minute car trip. Its Face image selects a child, so adult close-up acceptance relies on [iteration 23](REVIEWS/iteration_23_face.md), including its retained topology and numerical qualifications. The previous executable and archive remain at `dist/archive/JustLife-before-face-v23-j2py9o3p`; publication is recorded in `dist/game-work/face_v23_publication_receipt.json`. The adult face study was accepted as an incremental 7/10 improvement; the full-game rating remains 7.2/10.

The preceding Linux build is from commit **`75b91c0`**, adding the household calendar to the phone. The actual Forward+ executable passes **58 checks with zero failures, errors or warnings** on the available NVIDIA RTX 5090, using Dummy audio. All 152 runtime inputs and 991 archived source files match the exact commit. Compared with `f645987`, the runtime adds the two calendar scripts and their resource IDs, and changes the phone and opt-in release probe. All character artwork and other GLBs remain byte-identical.

Executable SHA-256: `94d2c4cc736e8be86cd541d9b5f3f676d1b58316ee9c7f5d7f7fe18d6c0c3f19`; Linux archive SHA-256: `dc17be532f47e66861e93e7b44cdaf0a42c922d22b3e0fef1837fbf35517e8fa`. The frozen handoff is `dist/test-work/justlife-package-75b91c0-3kah4620/handoff`. The packaging agent inspected all nine captures; root also inspected the calendar, adoption review and save picker. Five added assertions include the calendar capture, two-member school/work agenda, filtering, pause and complete household-state preservation. The previous startup, adoption cancellation, save/load/delete and real fifteen-minute car trip checks also pass. The separate eight-member calendar and native 960×600 UI evidence is recorded in [iteration 24](REVIEWS/iteration_24_calendar.md). The previous executable and archive remain at `dist/archive/JustLife-before-calendar-kr5_recm`; publication is recorded in `dist/game-work/calendar_publication_receipt.json`. That bounded package qualification left the full-game score at 7.2/10 and preceded the accepted face refinements.

The preceding Linux build is from commit **`f645987`**, adding the accepted adult clothing: a shaped casual shirt, continuous trousers and a cardigan hem adjusted for the reviewed poses. All 148 runtime inputs match the committed export snapshot, and only the four adult GLBs differ from the preceding `8b381be` runtime. The 981-file source archive, all 20 current character pins and the 15 unchanged nonadult files are exact. The actual Forward+ executable passes **53 checks with zero failures, errors or warnings** on the available NVIDIA RTX 5090, using Dummy audio.

Executable SHA-256: `d346972d808576ebeb8df49891109037ea669820668f58a8261b7237181b24f1`; Linux archive SHA-256: `30226597624675cbed68cb44b073fb034b4262a73e204e753561e3339c3a5fbe`. The frozen handoff is `dist/test-work/justlife-package-f645987-jie9tr1a/handoff`. The packaging agent inspected all eight captures; root also inspected the child face editor, Live household and save picker. The child model and adult faces remain unchanged. This package probe qualifies startup, imported assets, creator/family controls, saving and car travel; the separate adult clothing review is [iteration 22](REVIEWS/iteration_22_clothing.md). Its **7.5/10 garment score** leaves the **7.2/10 full-game score** unchanged. The previous executable and archive remain at `dist/archive/JustLife-before-clothing-v22-436cuh1f`; publication is recorded in `dist/game-work/clothing_v22_publication_receipt.json`.

The preceding Linux build is from commit **`8b381be`**, adding fresh save previews after panels close. It retains the two-storey, neighborhood, cooking and sanitation features and all v18 character artwork. The actual Forward+ executable passes **53 checks with zero failures, errors or warnings**. All 148 runtime inputs match the committed export snapshot; only `scripts/main.gd` differs from the preceding `49a1f1c` runtime. The 972-file source archive is exact.

Executable SHA-256: `30138b6b2ee640c0730403cdbfb4932a7d2da3f2193e2f5181ad77543964b8fc`; Linux archive SHA-256: `f72d8dac6c34b4ffad23c14556f1935f73a1f5c757edd6c471a99d7677146584`. The frozen handoff is `dist/test-work/justlife-package-8b381be-njjfoww_/handoff`. The packaging agent inspected all eight captures; root also inspected the corrected save picker, delete confirmation and library arrival. The picker now shows the Live home after the same rapid adoption-cancel/save sequence. Source reproduction and its retained baseline failures are in [the preview review](REVIEWS/iteration_23_save_preview.md). The previous executable and archive remain at `dist/archive/JustLife-before-save-preview-xemal8ho`; publication is recorded in `dist/game-work/save_preview_publication_receipt.json`. This is a bounded package qualification, not a new whole-game grade or another multiday playthrough.

The preceding two-storey Linux package was built from commit `49a1f1c`. It includes original stairs, landing rails and gable roofs, floor-aware activities and cleanup, saved journeys, transparent wall apertures, neighbor visits and wood floor materials. The actual Forward+ executable passes **53 packaged assertions with zero failures, errors or warnings**. All **148 runtime/asset inputs** match the then-current production source and the export snapshot, and all **966 archived source files** match the exact commit. It remained runtime-identical through `e73091c`; the later `8b381be` changes save-preview capture.

Executable SHA-256: `35703329d2d54a379b003f87153c369b714eb6b0f9c9b41f01599aa09fd66ac7`; Linux archive SHA-256: `5312d473968a74b7d706e44d2b9f0202486c0755925e3dda2ea71e904eb4677e`. The frozen handoff is `dist/test-work/justlife-package-49a1f1c-dix1e5cw/handoff`; exporter snapshot is `dist/test-work/justlife-release-aq86xoat`. The previous build is preserved at `dist/archive/JustLife-before-twofloor-mj6_fh3n`. Eight packaged captures were inspected. Four new probe assertions load the original architecture assets; actual upstairs construction, traversal, cleanup and fresh restoration are separately qualified by `REVIEWS/iteration_20_public_build_flow.md` and `REVIEWS/iteration_21_twofloor_sanitation.md`. That package retained the synchronous-probe thumbnail timing limitation described below, now corrected in `8b381be`. These results do not establish a whole-game 10/10 or other-platform support.

The preceding sanitation acting and mop-contact package from commit `83f9e7f` passes **49 packaged assertions with zero failures, errors or warnings** in the actual Forward+ renderer. All 116 runtime/asset inputs match the committed source and export snapshot; all 812 archived source files match that commit. Executable SHA-256: `35f04d1060ad50303a2f1990a23d68ff541d58b15f20251fae42c98174ba4d43`. The candidate and eight inspected captures are frozen in `dist/test-work/justlife-package-83f9e7f-ipwmbjn9/handoff`. The published executable and Linux archive were refreshed from those exact candidate bytes, preserving the earlier artifact under `dist/archive/`.

The package probe covers imported models, creator/family/adoption cancellation, named save/load/delete and actual car travel. The separate sanitation acting and full-duration mop evidence is recorded in `REVIEWS/iteration_19_sanitation_acting.md`; the package smoke test does not replay those animations. A rapid synchronous sequence in the probe leaves the canceled adoption-review frame in the save thumbnail until a render occurs; the household reloads correctly. That earlier package retained the one-storey source and did not include the subsequent two-storey and wood material integration.

The preceding neighborhood/sanitation Linux package passes **49 checks with zero failures, errors or warnings**, using the actual renderer. It loads the original car and mop from the PCK, completes a real household car journey with exactly fifteen game minutes, and verifies the creator, family, adoption-review cancellation, named save/load and confirmed deletion flows. Root inspected the actual home and destination captures. This package smoke test does not replace the207 directed sanitation checks or establish full-game quality.

Evidence: `dist/test-work/justlife-release-check-neighbors-gsnaln9e`; frozen exporter snapshot: `dist/test-work/justlife-release-o5y9ilpf`. All **116** recorded runtime/asset inputs match the committed source, including **48 GLBs**. Executable SHA-256: `922b565ae073f3360571528b2ce97cab994cae077816ca2792363daed153ec68`. The earlier attempt `justlife-release-check-neighbors-2r6mkfv3` completed captures but hung because the opt-in probe awaited after freeing its owner. The final probe schedules shutdown through SceneTree independently; this changes verification teardown, not ordinary game behavior.

8 September 2026 historical artifact: `dist/JustLife/JustLife.x86_64`, with three selectable recipes, original pasta/bake artwork and recipe preparation gestures, retaining shared meals and school/work responsibilities. It includes supported dish placement, autonomous washing, short courtesy walks and reserved standing-dining positions. The harvest bake now uses the original hollow oven, hinged door and sliding rack, with paused transfer/interior reconstruction. The phone now supports school-age adoption with a reviewed guardian choice, one-time fee, physical arrival and saved family/school history. At that revision, character meshes remained the reviewed v18 production assets; hair studies remained unpromoted.

## Packaged checks

The preceding adoption Linux export passed **45 packaged checks with zero failures, warnings or errors**, using actual Forward+ rendering on the available NVIDIA RTX 5090. Four added checks cover the three candidate controls, an eligible guardian review, its screenshot and cancellation preserving the household/funds. The opt-in probe pauses processing; it does not confirm an adoption, walk home or run school. Private data: `/tmp/justlife-release-check-adoption-nga0bteu`; exporter snapshot: `dist/build_snapshots/justlife-release-yydns02g`. Raw evidence is in `art/screenshots/adoption_iteration_17/packaged_release/`. All **104** recorded runtime/asset hashes match source, including all 46 unchanged production GLBs and all 20 character models. Executable SHA-256: `d203754c76e72fae1544a80571684e4038c7fd5126bb8f9208ebed76f4c2afc0`. The previous oven package and tarball are preserved in `dist/archive/JustLife-oven-v16/` and its adjacent Linux archive.

The oven Linux export passed **41 packaged checks with zero failures, warnings or errors**, using actual Forward+ rendering on the available NVIDIA RTX 5090. The probe additionally verifies the imported oven's rack/hinge anchors and closed-bake sequence. Private data: `/tmp/justlife-release-check-oven-wac7husr`; exporter snapshot: `dist/build_snapshots/justlife-release-5mg_ckjl`. Evidence is archived in `art/screenshots/oven_iteration_16/packaged_release/`. All **100** recorded runtime/asset hashes match the working source. Of the 46 production GLBs, only `stove.glb` changes; all 20 character meshes retain their accepted geometry. Executable SHA-256: `ecfb569534f933f89b06dd84c59f72c5f795a7bccde0f904aa6bbe5b7eb6ae66`. `dist/JustLife-Linux.tar.gz` was refreshed; the prior artifact is preserved in `dist/archive/JustLife-standing-v15/`. The opt-in probe pauses processing and does not perform the cooking animation; its separate gameplay evidence follows below.

The preceding standing-dining Linux export passed **39 packaged checks with zero failures, warnings or errors**, using actual Forward+ rendering on the available NVIDIA RTX 5090. Private data: `/tmp/justlife-release-check-standing-v2jzf291`; exporter snapshot: `dist/build_snapshots/justlife-release-ls3pmwuf`. Screenshots, import/export/runtime logs and the exact build manifest are archived locally in `art/screenshots/standing_iteration_15/packaged_release/`. All 98 packaged runtime/asset hashes matched that source revision, including all 46 then-unchanged production GLBs. The executable SHA-256 is `3a99667d33fa13331aab7a5f75425e1067db565c87a4be725c547dddebbb7847`; the adjacent `build_manifest.json` records its provenance. `dist/JustLife-Linux.tar.gz` was refreshed from this build. The probe pauses processing and does not exercise dining or courtesy movement; those have separate gameplay and component evidence below.

The prior supported-dish package passed 39 clean checks. Its evidence remains in `art/screenshots/dishes_iteration_14/packaged_release/`, from snapshot `dist/build_snapshots/justlife-release-ubuzrjlu` and private data `/tmp/justlife-release-check-dishes-h8rnrfia`. Its superseded executable SHA-256 is `d5742c58cde0d043afb6c88770028cdc0a8dc8d771b0a660a1bbf8430192d2ad`.

The prior recipe package passed 39 clean checks. Its evidence remains in `art/screenshots/recipes_iteration_13/packaged_release/`, with superseded executable SHA-256 `e6a3cb843074862307bab572699cdbe983fb834ac5b8f682f85590dbaf75a60c`.

The prior shared-meal package passed 35 checks; its evidence remains in `art/screenshots/meals_iteration_12/packaged_release/`. Its superseded executable SHA-256 is `15e09d70b914cb87f48243a5fd2f6c3756d66c230b710b60179e19f445998d61`.

The prior responsibility integration passed 32 packaged checks with clean logs; its evidence remains in `art/screenshots/responsibility_iteration_11/final_integration/packaged_release/`. The final meal build also reduces the sun's angular distance from 3.0° to 0.5°. The critic accepted the pictured stipple repair, with sharper shadows and residual edge aliasing; the matched still does not establish animated stability or all cameras. See `REVIEWS/iteration_12_meals.md`.

The earlier speech/v20 executable also passed 32 checks. Its first run reported two ObjectDB instances remaining at shutdown; a verbose repeat passed without that warning and recorded four hardware RGB8-to-RGBA8 conversion warnings. That historical evidence remains in `art/screenshots/release_speech_v20/initial_shutdown_warning/`. The intermittent warning is retained, not claimed fixed by the current clean run.

The probe checks startup/main menu, all five creator age choices, face controls, imported wordless voice/ambience/click resources, original cake/booster and all seven meal resources, child/adult move-in, directed genealogy, school enrollment, named save/load, Keep save, confirmed exact-save deletion and library travel. It does not measure perceived audio, sustained simulation or general hardware compatibility.

Historical speech/v20 input: `/tmp/justlife-speech-source-t0sm4kk1`; exporter snapshot: `/tmp/justlife-release-u6ywt6oa`. Its superseded executable SHA-256 is `22050b4a58910301964623257bccf9828a7efa20a57aa29713bc8391a4b8e102`.

## Connected gameplay evidence

The source repairs unfair resource access and blocked autonomous choices. Arrived waiters keep priority, wait at distinct reachable positions and reconsider urgent needs; an occupied resource remains reserved while its owner re-establishes physical arrival after loading. A typed-array runtime error found during the rejected candidate week is also corrected.

The final repeated seven-day, eight-member fixture passed **127 first-process and 129 fresh-process checks**, with no runtime errors. Every Lifelet finished 59–72 activities, the largest gap between completions was 12.38 game hours, and no critical needs or overlapping settled wait positions were observed in the samples. A separate public seven-waiter save/restart passed **154 + 25 checks**, verifying the current occupant resumes first and the oldest waiter follows after cancellation. These are fixture-specific observations, not a promise that every possible house has adequate capacity. Raw traces, rejected candidates and exact source hashes are in `art/screenshots/autonomy_iteration_10/`; independent findings are in `REVIEWS/iteration_10_autonomy.md`.

Cooperative homework now routes both participants, supports cancellation from either, preserves shared progress through a named restart, and awards learning/Parenting/relationship benefits exactly once. The HUD identifies the partner and displays skill progress. The helper raises an open explaining hand while the learner periodically looks toward them. The accepted directed child/adult run passed **72 + 23 checks** with no runtime errors; raw evidence is in `art/screenshots/cooperative_homework_iteration_10/accepted/`. Other ages/body extremes and smaller windows are not implied by that focused visual acceptance. See `REVIEWS/iteration_10_homework.md`.

The speech update repeats the connected homework flow, then checks readable named cards at three zoom levels, an actual 960×600 window, pause/modal restoration, offscreen removal and controlled simultaneous messages (**72 + 93 checks**). A separate **16-check** real-frame presentation probe verifies natural expiry after play resumes. These overlap the earlier homework checks and do not add new simulation breadth. The selected speaker is prioritized; excess crowd cards can be omitted. Source, images, exact hashes and limits are in `art/screenshots/speech_iteration_11/` and `REVIEWS/iteration_11_speech.md`. The package probe loads the new script but does not repeat those speech-specific scenarios.

The responsibility integration adds physical departure/return, persisted absence, once-only attendance/pay, autonomous school/work/homework and varied social contact. Its final rendered week passed **127 + 210 checks** with all eight members attending five due days; the final-source public career run passed **69 + 38 checks**. Lateness and prolonged low needs remain. See `REVIEWS/iteration_12_responsibilities.md` for the exact evidence, endpoint-test teardown caveat and limits.

Shared meals passed **100 first-process + 26 fresh-restart checks**, with no runtime warnings/errors, covering cooking, individual dining, actual furniture editing/cancel, leftover selection and washing. The separate maintained ledger test passed **101 checks**, the autonomous eating policy test **45**, and imported original prop checks **23**. The policy test retained two ObjectDB teardown warnings; it supplies controlled policy evidence, not a rendered household week. A subsequent three-day crowded baseline found floating and overlapping dishes; the supported-placement repair is now accepted with limits in `REVIEWS/iteration_14_dish_placement.md`. `REVIEWS/iteration_12_meals.md` records exact source hashes, contact measurements and visual limits. The packaged probe imports the meal assets but does not repeat that directed meal playthrough.

The recipe update passed **57 first-process + 28 fresh-restart checks**, with no runtime warnings/errors, covering skill locks, actual recipe selection/preparation/serving, distinct leftovers and a paid bake resumed beyond the original 45-minute cooking duration. Controlled recipe tests pass **103 checks**; exact repaired art passes **32 Blender + 42 Godot checks**, and preparation poses pass **120 checks over 435 samples**. These are bounded assertions, not independent complete game sessions. The public scenario uses explicit fixture skill XP; the package probe verifies all seven meal assets but does not repeat the full cooking playthrough. `REVIEWS/iteration_13_recipes.md` records failed attempts, independent review and remaining limits.

The supported-dish and idle-spacing integration passes **80 first-process + 29 fresh-restart checks**, with clean logs, on the final four changed runtime scripts. A separate recipe regression passes **57 + 28**. The final exported source also passes the maintained **49 placement, 8 floor-support and 14 courtesy checks**; only the courtesy log retains two ObjectDB teardown warnings. The three-day placement fixture predates courtesy and retains a failed late-dinner checkpoint; its separate copied-save continuation passes **74 + 61**. That iteration records active dining overlaps, slow cleanup, low needs and instantaneous setdown in `REVIEWS/iteration_14_dish_placement.md`.

The standing-dining integration passes **96 first-process + 35 fresh-restart checks**, plus **482 checks** replaying an authentic older crowded-dinner save. These rendered runs predate one final current-action identity guard, which protects the next instruction when dining resolution cancels synchronously. The final exported source passes **31 reservation/save, 11 later-arrival and 11 cancellation-route checks**; only the admission suite reports a shutdown warning for two remaining ObjectDB instances. All 46 production GLBs remain unchanged. Exact source provenance, observed spacing, failed attempts and limits are recorded in `REVIEWS/iteration_15_standing_dining.md`. These results address the earlier settled standing-diner overlaps in the exercised cases; crossing walks and full mesh collision remain outside this evidence.

The physical oven integration passes **34 + 36 + 45** checks over three actual rendered processes, saving during dish insertion and again during closed baking. It preserves paid time, one ingredient debit, one earned first-meal reward, the dish's exclusive ownership and the later reading instruction. A narrow resume-title correction passes two **20-check** fresh loads of those exact named slots; final-source recipe regression passes **57 + 28**. Final controlled controller/reconstruction checks pass **69 + 371**, and existing non-oven activity checks pass **250**. Earlier rapid controlled shutdowns reported two audio stream/playback objects; the final controlled tests allow a 0.2-second engine interval after freeing the scene and exit cleanly, without changing runtime audio or suppressing warnings. Directed 701-step motion checks cover default and short teen at 90 degrees, plus independently tested broad adult/elder cases; they do not prove all weighted-cloth or outfit clearance. See `REVIEWS/iteration_16_oven.md` for hashes, failed artifacts and visual limits. This is not a new multiday household qualification.

School-age adoption passes **75 first-process + 59 fresh-restart checks** in a four-member family flow. Existing paid oven loading and its later reading instruction survive cancellation, confirmation and a named restart with the arriving child selected. The child physically arrives, attends Day 2 once without lateness/absence and completes new Day 2 homework. The final typography-only adjustment passes **43 focused public checks** with actual 960×600 Window, native-client and PNG dimensions. The earlier combined images labeled 960 were actually 1440×900; they are disqualified as small-window evidence. Controlled transaction/arrival/oven checks pass **122 + 18 + 69**. An earlier school-return timeout remains unexplained; the passing combined telemetry does not establish its cause or a general robustness fix. See `REVIEWS/iteration_17_adoption.md` for exact hashes, fixture conditions and scope.

The independent full-request score remains **7.2/10**, with the 10/10 goal still open. Character acting/art, household depth and the wardrobe/build catalogue remain substantially short of the requested breadth. The current evidence does not establish feature parity with The Sims 4. The earlier full-scope findings remain in `REVIEWS/iteration_10_final.md`.

## Delivery and history

The original JustLife icon and wordmark splash remain configured with a matching cream background, preserved aspect ratio and 900ms minimum display time. The probe begins after scene initialization; it does not record the earlier native splash. Wordless character sounds remain original game assets and do not depend on GladeCore.

`tools/export_linux.py` strips development MCP services from the isolated export. It also skips the model studies already excluded by the export preset before copying/importing them. A temporary-storage quota failure occurred during an earlier snapshot copy, before Godot launched; the partial copy was removed and the successful final snapshot uses ignored build storage under `dist/`. Font and engine/component notices accompany the artifact. Staged candidate models and downloaded references are excluded. The adjacent `build_manifest.json` records exact runtime/asset and executable hashes.

The superseded care/v19 build is preserved locally at `dist/archive/JustLife-care-v19/`, with its original evidence in `art/screenshots/release_care_v19/`. The superseded family/v18 build is preserved locally at `dist/archive/JustLife-family-v18/`; its original evidence remains in `art/screenshots/release_family_v18/`. Its later 6.7/10 starvation finding is retained in `REVIEWS/iteration_09_autonomy.md`. Successful earlier focused tests did not establish a dependable household week.
