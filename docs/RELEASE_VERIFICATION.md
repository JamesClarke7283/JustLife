# Packaged release verification

8 September 2026. Runnable artifact: `dist/JustLife/JustLife.x86_64`, with embedded project data. Actual Forward+ renderer on NVIDIA RTX 5090.

The current family/v18 build passed **32 packaged checks, zero failures and zero engine/script errors**, then exited normally. It used private data in `/tmp/justlife-release-check-family-v18`; seven screenshots, runtime log, results and exact build manifest are preserved in `art/screenshots/release_family_v18/`. The reviewed input was `/tmp/justlife-release-family-v18-ohzzzlzd`; the exporter created its isolated snapshot in `/tmp/justlife-release-q3hnwun7`.

Verified in the executable: main menu; all five authored age stages in the creator; face editor; five imported wordless voice categories, ambience and click resource availability; original cake/candles and child desk booster resources; child-plus-adult move-in; directed parent/child setup and family tree; attainable child snack wish and school enrollment; named save/load preserving the family graph, per-member needs, schooling and lifespan settings; Keep save and confirmed exact-save deletion; and library travel. These are packed resource and public-control checks. Independent rendered activity, profile, birthday, desk, genealogy and fresh-process playthroughs are documented in `docs/REVIEWS/iteration_08_final.md` and its linked evidence. The packaged probe does not repeat every activity or imply a sustained-play or frame-rate benchmark.

This playable build contains all 16 reviewed v18 character assets, repaired facial attachment and crop hair roots, coordinated hand/prop contact, room-facing birthday presentation, parent/child genealogy, and the household context cache. The cache passed 46 invalidation checks; an isolated eight-member simulation benchmark measured mean household tick CPU time falling from 3.387ms to 0.592ms. This does not establish a rendered frame rate. A later seven-day rendered review found severe shared-bed starvation in this package: four of eight Lifelets waited for over six game days and reached zero needs. The current independent full-request critic score is **6.7/10**, documented in `docs/REVIEWS/iteration_09_autonomy.md`. Arrival fairness and autonomous replanning are being repaired in source; they are not yet included in this packaged executable. More natural hair and anatomy, sustained family gameplay and a broader wardrobe/build catalogue are remaining priorities.

The JustLife icon and splash are original project resources. The native boot splash displays the exact JustLife wordmark image, Cover with preserved aspect ratio, filtering, a matching cream background and a 900ms minimum display time. Configuration and packed startup passed; this probe captures after main-scene initialization and does not claim a recording of the earlier native splash.

`tools/export_linux.py` removes development MCP autoload/plugin configuration from its isolated snapshot. Font and engine/component license texts accompany the executable. The accepted models use the production resource paths; staged candidates are excluded from this build. Exact source and executable SHA-256 hashes accompany the executable in `build_manifest.json`.
