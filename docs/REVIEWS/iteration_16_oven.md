# Iteration 16 — physical oven cooking

8 September 2026.

**Accept this bounded oven implementation for integration. Full-game quality remains 7.2/10; this is not feature parity or a new full-scope score.** The original hollow stove, hinged door and translating rack now support a readable load, closed bake and unload sequence, followed by the existing serving/eating loop. The original 70-game-minute recipe, §52 ingredient price and eight portions remain intact. Hair and adoption candidates are outside this verdict.

## Evidence and provenance

- `dist/test-work/justlife-playthrough-_uq94z_b`: three actual fresh rendered processes, **34 + 36 + 45 checks, no failures or runtime warnings/errors**. I inspected the harness, raw results, logs and kitchen/reload/serving images, and verified all 354 recorded source hashes. The fixture starts an adult with Cooking XP 450 and autonomy disabled; it then uses public creator/menu/save controls and ordinary movement/time. This is directed flow evidence, not an autonomous household test.
- Real wallet events contain exactly one **−§52** at ingredient payment and one **+§60** when the previously unearned `first_meal` wish completes. There is one completed eight-serving bake, then one served/eaten portion: final ledger has seven remaining portions and one dirty plate. Actual callbacks complete cook, serve, eat and the previously queued reading instruction; the final queue is empty. No prematurely edible batch, duplicated fee or duplicate batch was observed.
- Both mid-transfer and closed-bake named saves reload while paused into separate processes with the paid elapsed time, actor position, route, queue and food state preserved. The transfer tray and world interior dish do not display simultaneously. Public snapshot comparisons cover positions and ownership; full transform/joint reconstruction is a separate controlled gate.
- `dist/test-work/justlife-playthrough-zgsege1u`: **20 + 20 checks, clean logs**, loading the exact two authentic `_uq94z_b` named rows. I verified its recorded hashes, the narrow `meal_flow.gd` difference and both final images. The restored HUD now correctly reads “Preparing harvest vegetable bake” in both poses. The correction affects the title only: paid preparation at its unchanged available host is recognized; a detached or relocated host still requires approach. The preceding full public flow was not rerun after this label-only change.
- Independently run r19 broad-adult and broad-elder motion evidence in `dist/test-work/oven-critic-r19-b2v87_1k`: **10,013 checks per variant**, 701 directed frames each, no failures. I inspected the broad/elder transfer and upright carrying images. Independently reviewed r20 default and short-teen 90-degree evidence under `dist/game-work/oven-source-gh7gu3n1/art/furniture/oven_evidence/physical_r20`: **10,013 checks per variant**, 701 frames each, all 41 manifest entries verified. The orientation correction interpolates gathering in oven-local axes; it does not relax a collision/contact threshold.
- Root-run final controlled gates, whose source/logs I inspected: **69 controller**, **371 reconstruction**, **250 non-oven activity checks**, all passing. Controller coverage includes paid ownership, waiting contenders, canceled/committed stove moves, rotation, sale and later-action behavior. Reconstruction compares fresh actors/scenes, joint origins/rotations and the actor/world dish's full transform, including orientation. These are controlled tests, not rendered public Build/cancel evidence.

The full-flow runtime uses actor `57da0030f7df2c3ddf427b9a312fff785fd437d5b07d08aa19427518778b603d`, oven sequence `7803b3932316c0c12a06a33f539be2647977526118a7a0a312289d2a8f0cd961`, world `9021a5b03a94abb89ed8109062fcd5c2f583e78a942a2e661746adbb948d3f94`, and main `54a338aee59f8c5b9cef7901b24804407eab2e6807303829f36a2f87387198bf`. Final title-fix meal flow is `424411265e05d7761ffc9267fc8a23724763ddc95997fc66c3efa5f4e566f5be`; full-flow meal flow was `b784508cd6ab408f0e377537809de7cb4575562fcb4a279fec576f0e18458fa8`.

## Visual judgment and remaining limits

The viewed kitchen poses show grounded feet, usable tray/body separation and believable contact with the extended rack, without the earlier knee/door and tray/face failures. Before/after paused-load images visibly match. This is a substantial improvement over the former seasoning/holding-only bake. The squat remains deep and shoulders stiff; this is competent stylized handling, not finished character acting. The stationary closed-bake segment is approximately **34.05%** of recipe progress; other closed-door transitions are additional, not stationary baking time.

Capsule/box, visible head bounds and palm measurements are useful conservative proxies, not exhaustive weighted-cloth, every hairstyle/outfit/body combination or all-angle collision certification. Frame-stepped animation checks are not hardware performance measurements. No new dense or multiday household run, public Build-interruption run or packaged executable certification is supplied by this review.

Original failed artifacts remain meaningful: `vzwiz4pb` failed two final wallet assertions that omitted the real wish reward; the corrected ledger gate above supersedes those assertions without altering gameplay. Earlier pose, route and reconstruction failures must remain labeled as rejected. Controller/reconstruction logs still record two ObjectDB instances at shutdown; root's verbose diagnostic identifies audio stream/playback objects. They are not gameplay failures in these runs, but must not be described as fixed until a separate teardown result establishes that. Clean public logs do not erase those retained warnings.

## Final receipt addendum

I checked `oven_controller_cleanup.log` and `oven_reconstruction_cleanup.log`: **69/0 and 371/0**, now clean even with verbose logging. The only test change adds a 0.2-second engine interval after the app is freed, allowing audio teardown before exit; no gameplay, audio runtime, assertions or warning filtering changed. This resolves the observed shutdown warning in these final controlled runs; it does not relabel the earlier warning logs or establish a general memory-leak audit.

The maintained recipe playthrough also completes on the same final five runtime scripts at `dist/test-work/justlife-playthrough-smns0tc8`: **57 + 28 checks**, both processes exit zero with clean logs. Its late-bake visual sample now occurs at progress 0.91, when the new oven sequence has actually retrieved the dish. This is additional recipe regression evidence, not a new visual or whole-game score.

The original oven authoring source and generator are retained in `art/furniture/oven.blend` and `tools/create_furniture.py`; `art/furniture/oven_manifest.json` pins their hashes. The unchanged r9 appliance also passes the 10-check static dish/cavity/rack support gate. This is geometry evidence, separate from character animation and public play. Screenshots and raw reports are retained locally in `art/screenshots/oven_iteration_16/`; the maintained portable three-process scenario is `python tests/run_oven_playthrough.py`.

The Linux package subsequently passes **41 checks with clean logs**, including two new oven anchor/sequence checks. Its opt-in probe pauses ordinary processing and does not replay cooking. `docs/RELEASE_VERIFICATION.md` records the exact executable and snapshot. All 100 packaged runtime/asset hashes match the final source; only the stove changes among the 46 production GLBs.
