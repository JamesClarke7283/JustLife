# Iteration 50 — give everyday routines more room

Normal speed previously advanced six game minutes per second. A short walk could consume much of the available preparation window, and ten game minutes of bladder urgency lasted under two real seconds. Normal now advances one game minute per second: a full day takes 24 minutes, or eight and three minutes at the existing faster speeds. Physical walking speed is unchanged. This is an intentional JustLife pacing choice, not a claim about a verified numerical setting in another game.

Needs, action durations, school/work hours, pay and deadlines still use the same game-minute values. A fifteen-minute activity takes fifteen seconds at Normal, and the ten-minute accident grace gives the player ten seconds to respond. Pausing and Build still freeze the simulation. Car travel still charges exactly fifteen game minutes at every public speed; its conversion now uses the clock constant.

## Observed household effect

Two runs load the same actual Day 2 08:08.4 checkpoint, preserving the complete decoded state and nine inherited player commands. At Normal speed, the original clock leaves both adults at home by noon. The one-minute clock allows Ellis to depart at 10:11.8, and moves Casey's and Morgan's school departures to 08:49.5 and 09:35.5. Rowan still misses work after painting, hygiene and rest. No commands, priorities, need values or route positions are changed to force attendance.

A separate complete-day observation at public speed eight confirms Ellis's actual shift completion, once-only §153 late-arrival pay and physical return. Both children complete their school day. The later sidestep search repair clears an observed overnight blockage using the same live prefix; that navigation change is documented separately in iteration 49.

The clock does not solve autonomous routines. In the day observation Ellis still reaches zero hygiene for 22.8 sampled game minutes and Casey zero fun for 36.4. Rowan's missed work remains. Longer activities also take more real time at Normal; the existing faster controls remain useful. These are practical limits, not a new overall quality score.

## Compatibility and tests

The two-runtime-file clock candidate passes 680 checks across simulation, household, school/career components, duration restoration, actual car travel at all speeds, sanitation with fresh save/travel, and actual offlot work departure/fresh return. The combined runtime with the sidestep budget fix also passes 980 checks across twelve additional activity tests, and 1,461 checks across the three maintained courtesy suites. The separate selector component passes 40/0. These are selected compatibility groups, not the entire repository test corpus.

Maintained tests that requested game minutes through a literal division by six now use the clock constant. Longer requested intervals are divided into chunks that respect LifeSim's existing sixty-second supplied-delta limit. Paused ticks, physical movement loops, activity assertions and rewards retain their original meaning. Chunking may change notification cadence; exact intermediate callback histories are not claimed. The rendered career test's wait uses the actual remaining time to the same shift-end endpoint, retaining its original margin and physical return checks.

The expanded story test exposed a deliberately selected Day 2 fixture that retained a fresh Day 1 career calendar. Actual baseline and candidate diagnostics each reproduce 75/2 and the same invalid-career-calendar rejection. Initializing that fixture’s career calendar for its selected day restores 75/0 without changing its assertions or weakening runtime validation. The corrected run includes one pure diagnostic print; the maintained test omits it. The original failed group and both reproduced failures remain recorded as failures. The school integration test that was skipped after the failure passes 64/0 in a separate continuation; the original whole group was not rerun or relabeled green.

The current artwork and autonomous priority policy remain unchanged. Further character design and household routine work remains open.
