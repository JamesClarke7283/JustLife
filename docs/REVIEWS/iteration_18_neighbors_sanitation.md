# Iteration 18 — neighbors, visits and sanitation

The independent critics accept the two bounded ground-floor features for combined verification: **neighborhood 7.5/10**, **sanitation 8.0/10**. These scores do not replace the last full-game assessment of **7.2/10**, and the requested 10/10 remains unearned.

Maya and Leo now walk past the player's home, leave between visits, retain stable relationship identities and own distinct furnished homes. Friendship unlocks a household car visit. The car journey walks each member to the curb, drives between locations, charges fifteen game minutes once, and restores the selected speed. Only physically present residents can be approached. The neighborhood critic reproduced and then verified repair of an earlier bug that rewarded a queued conversation after its resident had left.

Desperate Lifelets can use a plant pot at bladder 12 or lower. Ten game minutes actually spent at empty bladder produce a visible cleanable accident with hygiene and mood consequences. Physical events wait for visible Live actors. Puddles retain their location and household ownership; partial cleanup, competing cleaners and cancellation use the existing action controller. The original Blender mop has two-hand contact and a floor-level sweeping head.

## Independent evidence

- Neighborhood review: `dist/test-work/neighborhood-critic-fixed-i24ym28l/evidence/NEIGHBORHOOD_CRITIC_REVIEW.md`, SHA-256 `3949cb6c96412ada16779c793074ebfb52fbb8877e7d72f1a6b208038c8abc80`. The repaired producer suite has **204 checks / 0 failures**; three processes retain shutdown ObjectDB warnings. The independent queued-absence reproduction no longer grants friendship. The five inspected runtime captures show walking, car departure, both different homes and their map addresses.
- Sanitation review: `dist/test-work/justlife-sanitation-critic-o_t74uxc/evidence/SANITATION_CRITIC_REVIEW.md`, SHA-256 `d2d96e99139c4beb20db0efbd61bb926faf31f7df5f35ac5679f95fda0427074`. The frozen source passes **80 headless** and **83 rendered** producer/fresh-load/cooking checks. A separate **25-check** independent run repeatedly loads active cleanup in the same process, verifies obsolete nodes free, completes cleanup once and completes the later reading instruction. These runs are clean. Five exact-source runtime images show pot use, rug and wood accidents, mopping and restored floor state.

These local evidence paths identify retained review records, not required checkout files. Maintained runners produce fresh logs in private projects and save folders.

## Remaining quality work

Residents currently follow two fixed profiles and simple visible routes, with no full offscreen needs/career simulation. Car passengers appear and disappear at the curb; door and seating animations remain absent. Friend homes differ in layout but reuse the furniture collection. The sanitation critic found wet patches subtle under shadows and requested clearer hover feedback plus more expressive urgency and accident reactions. Autonomous cleaning and richer environmental consequences are absent.

The separate two-storey build is not qualified by these ground-floor reviews. Its stairs, physical supports, cutaway and staged save adoption need their own integration evidence. The later package gate below is separate from these feature reviews; no full-game 10/10 approval is implied.

## Root combined verification

The current combined neighborhood/sanitation runtime passes **238 neighborhood checks** in `resident-checks-nc8afy3b`, including an eight-member visit, named load and return. The in-memory home remains exact before JSON serialization. After load, decoded construction records and original rendered wall/floor geometry are exact; furniture positions are exact. Euler-angle reconstruction produces at most `5.960464477539063e-8` error in a float32 basis component, below one `FLT_EPSILON`. Earlier deliberately stricter double/basis equality failures remain recorded; the check now identifies the actual storage and geometry contracts separately.

The combined runtime also passes **140 backend/path/import checks** in `justlife-storage-pr6zrv7e` and **88 actual-renderer public menu/save-picker checks** in `justlife-playthrough-8k4oer8e`. Recorded runtime inputs match the current source. The named-slot writer now requests full JSON precision, with a regression for the observed wall-depth value; this does not claim every native JSON double round trip is bit-exact.

The final merged sanitation suite passes **207 checks** (57 producer, 14 fresh consumer, 9 real cooking/carrying, 127 car/location integration), all clean, in `justlife-sanitation-_h64c_1g`. All121 recorded production/test inputs match the merged root exactly. The car gate traces real boarding/departure/arrival, confirms one fifteen-minute charge with no physical accident during travel, then follows four actual accidents across home/Maya/Leo through named loads and cleanup exactly once. The old fresh test's immediate-travel assumption caused two retained failures in `justlife-sanitation-jmpgnmnx`; waiting through actual controller travel resolves those checks without a runtime change.

The neighborhood's two combined-run shutdown warnings disappear in focused fresh/queue replays after the tests allow the audio mixer150ms to drain; the producer also passes65/0 clean with that teardown. This is a test shutdown timing change, not a claim of a general audio-resource repair. Their runtime inputs are unchanged.

The final Linux package passes49/0 clean using actual rendering, including packaged car/mop resources and a real fifteen-minute trip. See `docs/RELEASE_VERIFICATION.md` for source/executable hashes and the retained verification-teardown failure.
