# Iteration 39 — Child Bob lower-detail hem

The child Bob had narrow dark triangular marks along its lower edge in the standard and broad lower-detail Creator rear obliques. A cap-only export-order correction makes that edge more continuous. Native source and both full-detail GLBs stay byte exact; no character anatomy, face, fringe, garment, rig, material or runtime code changes.

The original cap stack subdivides the accepted surface, creates a 7 mm inward shell, then collapses the finished shell to a 0.12 ratio. The new LOD stage performs that same reduction before creating the shell. The original evaluated cap was closed and manifold, so the marks were not established to be literal mesh holes. The matched export-order comparison supports the changed reduction/shell interaction as the useful correction; it does not identify every individual renderer shading contribution.

Only `Hair_Bob_Cap` differs in the two final LOD GLBs. Every other one of the 338 decoded mesh records and its protected accessor sharing remains exact, as do root/node/material/rig data. Native topology, geometry, UVs, identities and weights remain byte exact through an unchanged `.blend` file. The LOD cap’s evaluated topology, normals, UV interpolants, indices and storage may change through the declared modifier order; no unrelated data tolerance is permitted. The unchanged v36 guarded transfer function copies its existing 25 Bob primitives from the Blender raw export, followed by a stricter cap-only final comparison. That retains the accepted unrelated Crop UVs despite reproduced raw standard-LOD export drift.

The new maintained stage is `tools/child_bob_lod_hem`; all v36 source tools and standalone CLI are unchanged. It extracts five hash-pinned accepted inputs from local commit `5ee38d2fd1dde81ee4c1dcb01bd0cf9c01597dc4`, reopens the native source in two separate single-thread Blender processes, and requires both raw and final LOD output hashes to match the reviewed files. The native source and two full-detail GLBs are copied byte exactly. The driver records logs, helper/script/input pins and exact final mesh/sharing witnesses, and refuses existing or overlapping output destinations. No private artifact directory is required to reproduce the stage.

The matched art fixture uses exact runtime commit `49221392a7b59321ee89cb1836052e7e827100ea`, a saved layout with stable IDs, 1440×900 Forward+ graphics, fixed light/camera/pose/RNG, and all Node input modes excluded, including newly added nodes. The initial four Creator pairs cover opposing rear obliques in standard narrow/short and broad wide/tall supported frames with all four identity sliders combined at maximum. Both complete reports match byte for byte: 33/0 per side, four images and two successful observed WeakRef drains.

Six additional pairs comprise two maximum-identity LOD fronts and four ordinary Live friendly head-motion samples (steps 60 and 236), two per body frame. Live uses the public camera handlers to zoom 7/elevation 0.35 and production LOD selection, with a deliberately held rear-oblique actor yaw to expose the hem. Household time/root position remain paused while the ordinary pose advances. Hair remains visible above the identical foreground plant; lower-body occlusion does not establish body or gameplay quality. At roughly 30–35 pixels tall, those heads provide ordinary-play appearance evidence; Creator views carry the detailed hem assessment. The rear views do not establish visible blink appearance despite the recorded blink state. These reports also match completely: 59/0 per side, six images and four successful drains.

All four graphics receipts retain failed strict status for the identical verbose RGB8→RGBA8 startup warning, present in each unchanged baseline as well as candidate. Imports are clean; no quieter warning-suppressing rerun is substituted. The Blender environment’s installed-addon `cattrs` traceback is also retained; successful output reproduction is not a clean-addon-log claim. Initial failed source introspection and all earlier v36 art/capture failures remain archived.

The cap changes from 3,594 to 3,724 triangles (+130, approximately 3.6%), adds 61 exported vertices and 2,732 bytes to each LOD GLB. These are decoded geometry/storage counts, not a measured frame-time benefit or cost. The narrow dark teeth are reduced, while a smooth heavy rear mass and broad angular hem corners remain. This is an incremental local cleanup, with no hairstyle-wide or whole-game score uplift.

A fresh portable reproduction passed with both raw and final LOD GLBs byte exact to the reviewed candidate, native and both full models byte exact, and the 338-mesh protection checks satisfied for each LOD. Its two Blender runs finished successfully with the installed-addon traceback retained. Final independent visual/source closure is recorded with the private promotion handoff. Root owns current-source integration and publication.
