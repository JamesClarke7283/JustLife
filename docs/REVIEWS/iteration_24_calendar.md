# Iteration 24: household calendar

The phone now opens a shared calendar with day selection across the next two weeks and independent school, work and birthday filters. The agenda uses each member's existing enrollment, career attendance, queued actions, absence and aging settings. It shows completed or active attendance and distinguishes the journey home. Adoption keeps its existing availability and confirmation controls below the calendar entry.

Birthdays are forecasts under the current aging settings. Automatic aging off produces no predicted birthday, and elders have no unsupported next-stage birthday. Future routines stop at the next age transition; opening the calendar after a birthday uses the new life stage. This is an agenda for existing household routines, not a social-event scheduler or holiday system. Browsing changes no saved gameplay data and restores the speed that was active before the phone opened.

The final isolated source run is `dist/test-work/calendar-check-8xtlzr83`, frozen by `FROZEN.json` SHA-256 `8e7707b928df3269b2df82942b8633dd4ccc8ce3035198cd96ea2422de158176`. Import, **19 data checks** and **38 actual rendered public-callback checks** finish cleanly with zero failures, errors or warnings. All 189 recorded inputs remain exact. The eight-member fixture covers two pupils, six adults, an expected birthday, a maximum-length name, a queued reading instruction, filtering, week navigation, scrolling and nested pause restoration. The window, native client area and image are verified at 960×600. Five images are retained; these are bounded UI scenarios, not unscripted play or a new full-game score.

Independent source review found agenda-title overflow and a late-night time-label boundary. Titles now use ellipsis with a full tooltip; time formatting stays within its displayed date. The first rendered candidate passed its assertions but revealed a separate fractional-aging artifact: an exact 10:00 forecast was shown as 09:59. The final source snaps only sub-micro-minute arithmetic residue at an integer-minute boundary, with a directed test and corrected screenshot. Both earlier runs and the original review remain at `calendar-check-nf167g77`, `calendar-check-is4gtax_` and `calendar-source-critic-mwx7jl2f` under `dist/test-work`.

The accepted source hashes are `calendar.gd` **5c659f118f3d8b6fb764061ea4a7c299a02c8dd233145b9f65fa6a3f49493d20**, `calendar_panel.gd` **9d3cafc17bb21a08154f08d2a84ac49b5322900c0ad46b20b1791d08e61ec904**, and `adoption_flow.gd` **e98f33e1530fd9e015ff9efd2741118aa545629445c8ab4ccdf740103922e9de**. No simulation, actor or artwork file changes. The opt-in release probe separately adds a packaged agenda image and checks its two-member plan, filter, pause and state preservation; its exported result belongs in `RELEASE_VERIFICATION.md` after packaging.

Independent final UI review accepted the five inspected images within this scope at `dist/test-work/calendar-ui-critic-5t7ki68l/REVIEW.md` (SHA-256 **683acd3560b90d2434e42f3c5c1e175595ff1f5690712367aeb1d643d17ecaa2**). The critic rehashed all 189 inputs and confirmed the three production sources match the tested snapshot. The UI fixture invokes public button signals; it does not establish an independent pointer or keyboard-accessibility audit. The full-game score remains 7.2/10.
