# Iteration 52 — casual drape and shoulder weights

The final adult clothing pass improves the casual shirt hem and shared trouser contours, then corrects the way the shirt follows raised arms. The former binding assigned substantial arm influence to the lower torso: raising both arms pulled the shirt's sides into large visible wedges. Surface-aware weights keep the lower shirt with the torso and the sleeves with their corresponding arms. Cuffs and trim receive weights from the resulting shirt surface.

The first stage moves seven garment meshes, including shared trousers and their seated correction. The second changes only six casual-shirt/trim weight arrays. Body geometry, head, mouth anchor, rig, materials and the Jacket/Cardigan garment meshes remain protected. The fifteen nonadult production files are unchanged.

Independent review accepts the combined result as an incremental improvement. Both adult frames retain the torso repair, attached sleeves/cuffs and calmer hem through the reviewed reach, walk and seated poses. The other outfit views show no conspicuous new damage. Small exposed inner-arm strips, a rough shoulder transition, padded shoulder styling and mottled seated trouser shading remain. The simpler models also show more angular chest shading. This is not a 10/10 character or whole-game rating.

## Verification

Fresh native reopening verifies all 362 objects, material values and a usable scene. The full-model geometry predecessor passes 900 strict checks. Comparing that predecessor to the weight correction passes 936 checks, allowing only the six intended skin-joint/weight arrays while retaining every geometry attribute and 333 unrelated meshes. The separate final LOD qualification passes 1,072 checks, preserving 332 unrelated meshes, scene structure, materials and rig while validating the changed garment arrays, triangle indices and normalized weights.

The earlier strict LOD comparison remains a recorded failure: 1,824 checks with 46 failures. Its exact derived-channel expectations do not accommodate the decimator's changed vertices/UVs/joints/weights on the six reshaped shirt/trim meshes. The final semantic checks are a separate qualification, not a relabeling of that failed run. They require finite aligned attributes, valid triangles and skin indices, preserved schema/sparse metadata, and normalized nonnegative weights. There is no invented 0–1 UV constraint.

The same 28 held-pose views cover both frames, front/profile reach, walking, seated casual and alternate outfits. Full and LOD-payload captures each pass 3,231 assertions with clean Godot logs and complete controls byte-identical to the predecessor. The LOD run substitutes the actual simpler model payloads into the capture paths; it does not test automatic distance switching. These are controlled production-animation poses in the Creator, not complete enacted shower/chair journeys or continuous cloth collision tests. The presentation runtime is the same frozen `94bcbf3` baseline on both sides.

Fifteen of sixteen alternate-outfit full-detail images are byte-identical to the predecessor. The broad Jacket seated-front image differs at 325 cuff-region pixels; the actual pair was inspected and retained. No unsupported cause or claim that all sixteen images match is made.

The compact `tools/adult_casual_drape` chain retrieves its pinned original-art input from Git, performs both authoring stages, reopens the native result and exports all four adult models in isolated directories. The relocated reproduction completes all seven stages and produces the four exact accepted GLB hashes. Both object-fact files and the drape report match exactly. The weight report retains its actual intermediate-source hash; comparison substitutes only that named provenance value before checking the reserialized report. Saved Blender container hashes differ with relocation, while recorded native values and exported bytes remain exact. Blender startup/export diagnostics remain recorded.

Evidence is local under `dist/test-work/adult-casual-weights-final-dubqvk6y`, `adult-casual-weights-posed-6sr3gylq`, `adult-casual-weights-lod-posed-hizdn313` and `adult-casual-reproduction-v7iwh1gi`. Large reports and captures are excluded from source control. This was the single final candidate allowed by the user's bounded wrap-up request; no further visual study follows it.
