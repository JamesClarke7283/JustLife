# Clear camera controls and household selection

9 September 2026. The independent critic accepts this focused UI improvement. It closes the observed tiny camera-symbol and low-contrast household-label findings; it does not change the full-game score.

Five original SVG icons replace the small font glyphs for creator rotation, Live camera rotation/zoom, and the pause menu. The existing button positions, sizes and callbacks remain unchanged. Icons use dark strokes over the normal cream background and white strokes over the pressed teal background. Descriptive tooltips and accessible names identify each action. The household selector has a cream background sized for its member chips and a dark caption, so scenery cannot obscure its label.

Root and critic directly inspected four actual Forward+ captures: Creator and eight-member Live at both 1440×900 and 960×600. The window and captured image sizes agree. All eight chips fit with margins, and the symbols remain recognizable at the smaller size. Source import and the 19.41-second rendered capture completed without diagnostics. Recorded public-button signals preserve creator rotation, camera rotation by 45 degrees, zoom steps of 1.5 and opening the pause menu.

Evidence is retained at `dist/test-work/camera-controls-fflwgs0r`. Its 26-artifact freeze SHA-256 is `375491344bc72579fce9d45223e15f8465756d5fa31af5eb691f121296cc5176`. All 1,054 other original source files matched `bce9269` before the postcapture curated input-list update. The independent review is `dist/test-work/camera-ui-critic-bce9269-ebqq85j2/REVIEW.md`, SHA-256 `21bcb25be7cfcc796e697a2ef1dfa10b7a14b277a13c4e8ad0d9ad3cb40a699e`.

The final helper adds one nonvisual line after those captures: `accessibility_name=hint`. The critic separately reviewed this addition; the [current Control documentation](https://docs.godotengine.org/en/4.7/classes/class_control.html#class-control-property-accessibility-name) confirms its assistive-app purpose. The curated regression inputs include all five SVGs and import settings. Five existing playthrough fixtures now find the icon-only pause control by its stable `PauseMenu` name, using the same visible/enabled checks and public pressed signal. The promotion receipt is `dist/game-work/camera_ui_promotion_receipt.json`.

These captures and recorded callbacks do not verify native Shift-drag, mouse-wheel input, keyboard focus/hover operation or screen-reader output. General text sizing at 960 remains a separate accessibility limitation. The surrounding character models and world are unchanged; the eight-member initial lineup is a paused setup, not evidence of later household spacing or autonomy.

The final combined tree/UI source subsequently passed a fresh import and the existing rendered menu suite: 88 assertions, no failures or diagnostics, seven actual images, and no source/runtime input drift. The stable `PauseMenu` lookup succeeded eight times. Blank names, five coexisting slots, long household names, Keep save, exact confirmed deletion and unreadable-save recovery passed. The critic inspected all seven frames; root inspected the paused eight-member HUD and long-title delete confirmation. Root verified all 1,715 frozen artifact pins and all 1,077 canonical source files before this documentation addition. Evidence is `dist/test-work/combined-menus-final-uk_4aura/handoff/FROZEN.json`, SHA-256 `21b6b963d1643cbd81f0d43a3d7ebc55dac1177e232f1125c35f9b923e502b0e`.

That suite drives public signals with viewport and Node input disabled. Its Escape check invokes the production handler directly; no native keyboard propagation is claimed. The seven images include a 1120×700 picker; the separate four-frame study above covers 960×600. This suite does not start a fresh-process resume. Its earlier weaker-isolation run remains preserved as superseded evidence.
