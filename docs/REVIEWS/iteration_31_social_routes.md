# Iteration 31 — social conversation routes

A directed household review found neighbors could attract conversations whose endpoints were too close to their bodies. Real movement then stopped before arrival. A later target refresh could also replace an action endpoint while its owned route kept the original endpoint, causing the named-save validator to reject the mismatch.

Social actions now share a supported, body-clear endpoint selection path. An admitted endpoint remains exact while it is still usable. A moving housemate or neighbor causes re-admission before further conversation progress, retaining the action's paid flag, elapsed work and later queue. The existing 0.78 route clearance and 0.72 body gap remain unchanged. Refusal uses the existing cancellation path and cannot earn a social result.

A caller already crossing stairs finishes that protected crossing before re-admission. A target that is using stairs is deliberately refused with a message asking the player to try after they reach the next floor. This does not preserve the conversation for later; no new saved waiting phase is claimed. Cooperative homework actions are excluded from this reconciliation.

The final private source overlay changed only main.gd against archived bce9269. Its main SHA is `e149dd4922d9a553ddeb4c15200af2c89e163aa82ff6f47a21161514bcc1d4ba`. Root owns application over the later UI/tree source. The frozen handoff is `dist/test-work/three-day-composition-bce9269-spflr_jw/social_review_v1/`, whose FROZEN.json SHA is `b954239a4b532763600ef877834f01bdfa14b67b3fa7d13f97d4e1cb9b28fdad`.

The complete maintained runner, `python3 tests/run_social_navigation.py`, was qualified end-to-end on that exact overlay: clean import, producer 192/0 and fresh process 58/0, with zero engine diagnostics or input drift. The producer covers curb admission, exact action/route identity during ordinary refresh and Build entry, a paid partial conversation retargeted after movement, blocked refusal followed by a completed later instruction, caller and target stair cases, and a legacy park route. Fresh loading checks three actual named saves: curb approach, active partial household chat, and moved household approach. Each preserves its decoded state through paused steps and then reaches one actual completion without duplicate rewards.

These are controlled fixtures: target/body placement and disabled autonomy isolate collision behavior, while initiator movement, activity time and rewards use ordinary app steps. Public Creator/move-in starts with default funds. Construction clicks and a catalog furnishing placement signal exercise production validation and charges; native pointer picking and rendered motion are not qualified. The runner isolates XDG data, config, cache, saves and temporary files in a retained private copy. Stable admitted endpoints return before a route search, but multi-target route-search cost is unmeasured.

Earlier failures remain visible. The initial composed run was 97/14 and yielded only every ten simulation steps, delaying deferred callbacks. A qualified per-step baseline reproduced the curb/save problem. One intermediate control run reported 183/0 assertions but emitted 21 script errors from a typed-empty-array legacy branch, so it is failed. That small type repair received a clean final legacy check with exact canonical occupied-array bytes and actual park completion. Two fresh attempts then exposed an invalid test resident phase at home; the test now uses the supported walking lifecycle and verifies exact snapshot/restore facts. The final standalone run supersedes those fixtures without rewriting their retained evidence.

The broader three-day household composition, attendance and needs balance remain a separate review. This repair does not establish a whole-game grade.

Root applied that exact runtime patch over the current camera UI and field-maple source. The merged main SHA is `d11af6668756439b9f26ad7dd9358ac70823eda18c197e4214e8a414a02f8a6b`; the sole new-test adjustment uses the existing `PauseMenu` control name. The maintained runner then passed a clean combined import, **192/0 producer and 58/0 fresh**, with all 244 input pins unchanged. Evidence: `dist/test-work/social-navigation-check-2ld9syj1/COMPLETED.json`, SHA-256 `8d17d4dad8acf00d49a444697133891a38c17ea4028dda7c76d098d67b57344f`. Root verified its 257 frozen artifacts before committing.

The independent final source/evidence review accepts this bounded repair with the stair-refusal and performance limits above. Review: `dist/test-work/social-final-critic-930p6sh6/REVIEW.md`, SHA-256 `553bbb7995756da28c8ce3610891bac51e6afc0815c6aed950875a62b0e5776a`. The critic verified the original standalone run; the subsequent combined run is separately recorded here.
