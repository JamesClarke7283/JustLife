# Iteration 57 — crowded-home autonomy, wall paint, shared seating and the iteration 56 repairs

10 September 2026. Reviewed commits `790c3eb` ("Vary crowded-home leisure, paint walls, seat two on the loveseat") and `aa32fbe` ("Balance a crowded week: queues, pastimes, emotions and the hood") together against `93e370e`: load-aware leisure with a three-entry pastime history, the forty-five-minute queue rule for physical needs, Fun projected across a school or work day while it is being prepared, a wall paint tool with five swatches and undo, per-seat loveseat resources, nearest-free-seat television, disabled age-gated menu entries, start-time cancellation of age-gated actions, the emotion/activity combination for skill growth, a Confident moodlet after an outfit change, the softened Fun drain of a day away, the V stretch, the long-hair back sheet re-authored over the hood roll, and the busy-resource harness re-timed to the current clock. During the review the working tree drifted again (uncommitted edits to `scripts/actor.gd`, `guest_meal.gd`, `home_visit.gd`, `residents.gd`, `world.gd`, `tests/README.md`, `tests/test_catalogue_week.gd` with a `JUSTLIFE_WEEK_DAYS` knob, and three home-visit/pedestrian tests). Those edits are not scored; every critic rerun below used a `git archive HEAD` export (`aa32fbe`). **Full-request weighted score: 7.6/10, provisional.** The critic did not play the build; behaviour claims rest on the diff, the developer's three rendered catalogue weeks (runs 3–5), pinned reruns of the regression groups, prework and busy-resource runners, nine direct headless suites, and a 131-check probe written for this review. Visual claims rest on the developer's frames and crops and the re-rendered Blender wardrobe sheets.

| Dimension | Weight | Score | Evidence and remaining gap |
|---|---:|---:|---|
| Visual and character quality | 20% | 7.2 | The production-camera close-ups requested twice before now exist: the adult seated at the chess table in shorts from behind (bare legs, hood roll, stool contact all read), the bather's torso above the bubbles, the child at the toy chest, two Lifelets side by side on the loveseat, and the stretch on the mat. The hair sheet no longer passes through the hood roll in any rear sheet (adult, child, elder checked). Against that: the raised-arm stretch still shows a torn, hatched patch on the raised sleeve's shoulder in both `iteration57_closeup_stretch_hoodie_crop.png` and the `_v2` recapture (the V raise reduced it, it did not remove it; the jacket/hoodie shells keep the older shoulder weights by the developer's own admission); the re-authored long hair now hangs as a boxy dark slab behind the head rather than a drape (clearest on the child rear sheet); the mirror is still a flat white oval; child, teen, adult and elder still share one face; at the production camera the loveseat pair are roughly 40 px tall. The wall paint reads cleanly in `iteration57_paint_wall.png` (sage north wall, cream elsewhere). |
| Usability and flow | 15% | 7.6 | Paint wall: a tool button, five tooltipped swatches with the active one ringed, "Choose a colour, then click a wall to repaint that segment.", "Point at a wall on this level." for open floor (probe and frame), "That wall already has this colour." for a repeat, an undo that peels coats one at a time (probe). Age-gated entries now appear disabled with their reason rather than as an empty menu (`get_actions_for` returns the jog/toys entry with `available=false` and the reason; `main.gd:1406-1408` disables the button and puts the reason in the tooltip). The Career details line now keeps "Next: <skill> <level>" because the only adult writer (`main.gd:796`) composes it (by inspection; not imaged). Unchanged gaps: no 960×600 capture (the Structure row now holds six buttons and five swatches on one line); the paint tool has no room-fill or both-faces option, so a four-wall room is four clicks and four undo entries. |
| Simulation and interaction depth | 25% | 7.8 | Verified by probe: with one easel busy and a piano free the second adult plays the piano (easel load 90 min); a five-minute queue at the easel still loses to a free bookshelf; with only a busy easel the Lifelet still queues; the unloaded order is paint > read > watch > piano; a free home rotates paint, read, watch, piano, paint, read; a six-hour bed queue yields a sofa nap while a forty-minute one is waited for; with everything loaded the least-loaded option wins; at 07:00 and 08:46 a Creative adult at Fun 46 relaxes before work; a child at 06:30 does the same; a child's jog appended past the queue is cancelled at start with the reason and grants nothing; toys queued before a birthday are cancelled for the new teen; reading is 31.25 Logic with no moodlet, with a Happy moodlet and while Unsettled, 25.0 when Tense; an outfit change gives "Freshly changed" (Confident, 120 min) and a rehearsal then earns 34.5 Charisma against 30. Run 5 (rendered, eight Lifelets): every purchased furnishing used (jog 4, toys 10, piano 4, bath 4, dance 3, stretch 3, speech 8), five non-Active adults each with at least three different pastimes, waiting 0.5–6.3 h per member (run 2: 7.8–11.2), 5/5 shifts and school days, 0 late minutes, no need at zero. Gaps: a Lifelet whose sleep ends after 09:00 leaves at Fun 45 and comes home at 10 (run 5 Morgan, minimum 8.4; probe at 09:05 ends the shift at 18.3 even with the softened drain) because the projection is only consulted before the duty is due, although arrival stays on time until 10:00; the pastime rotation reaches into the pre-work list, so at 08:40 with relax/read/watch as the last three pastimes the "brief" break is a 90-minute canvas and the Lifelet is 10 minutes late (probe); the third watcher prefers queueing at a full loveseat over a free armchair further from the set (by inspection, below); the pastime history is not saved; the last tuning (`aa32fbe`) has no rendered week behind it. |
| Creative breadth and sustained play | 25% | 7.7 | Build gains its first surface tool since floors: per-segment wall paint in five colours, priced by length, with history, on both legacy starter homes and canonical rooms (probe). The loveseat finally seats two and television seats a household. Otherwise the same 39 furnishings, eight skills, eleven activities, one career track per Lifelet, three venues and one face per family as iteration 56; no wallpaper, no room-wide finish, no new careers, venues, relationship content, ages or body types. |
| Reliability and delivery | 15% | 7.5 | Critic reruns on the HEAD export: regression groups build (3), protection (2), waiting (1) PASS; prework 40/40; busy resources with the re-timed windows: import + ten phases (32, 20, 20, 41, 29, 34, 33, 60, 54, 42) with 0 failures and no drift; direct suites test_build_transactions 47/0, test_autonomy_policy 167/0, test_waiting_autonomy 11/0, test_simulation 51/0, test_construction 21/0, test_progression 13/0, test_world 248/0 (6 usability observations), test_actor 612/0, test_build_history 10/0; probe 131 checks, 2 failures, both reported below. The developer's pre-session logs confirm the busy-resource failures reproduced on `ec83a80` and `93e370e` (`busy_resources_base.log`, `busy_resources_head.log`: the same two checks), so that was a stale harness. Run 5 frame time over 37,757 frames at very fast: mean 33.4 ms, p50 28.7, p90 62.4, p95 75.5, max 138.5 (run 2: 40.1 / 33.5 / 73.3 / 90.3), an improvement that is not a controlled comparison. All 20 pinned hashes in `art/source/character_production_hashes.json` match. Costs: no packaged build; no 960×600 capture; the tree moved twice during the review; the final commit's Fun drain, moodlet and emotion changes were never run through a rendered week; `construction.gd`'s no-quote legacy path turns a paint proposal into a wall removal (latent, unreachable from the shipped UI). |

Weighted mean 7.58. Of the iteration 56 P1 conditions: the idle-furnishing finding is repaired and proven in a rendered week (every furnishing used, one non-Active adult varying leisure, waiting inside the developer's six-hour bound though not the critic's four); the Fun-collapse finding is repaired in the preparation window only and the suite retains that failure; the ±90° and corner wall-decor cases pass (painting on the east wall with a parallel wall 0.7 m in front accepted; painting, clock and shelf snap to placeable spots in the north-east and south-west corners; an upstairs east-wall painting accepted).

## What works

- Load-aware leisure (`life_sim.gd:_autonomy_need_choice`): `_autonomy_target_for` now returns the queue load and the Fun branch scores `load+40` for any busy target, `5×index`, and `+25` for a pastime in the three-entry history, taking the favourite outright only when free and fresh. Probe: LOADED choice `play_piano` at load 0 with the easel at 90; BRIEF-QUEUE `read` with the easel five minutes from free; UNLOADED `[paint, read, watch, play_piano]`; ROTATION `[paint, read, watch, play_piano, paint, read]`; a bored child takes the toys first and the second child does not queue behind the busy toybox.
- Physical needs keep their order and fair waiting under forty-five minutes: bed with 360 minutes left → sofa nap; bed with 40 left → sleep at the bed (load 40); bed 100 / sofa 75 both loaded → the sofa (least loaded); shower with 30 left → shower. `test_waiting_autonomy` 11/0 and the busy-resource phases confirm the directed and thirty-minute waiting contracts still hold.
- Fun projection while preparing: `_autonomy_projection_need(preparing, 60, duty.is_empty())` includes Fun only before the duty is due and only when the projection is under 20. Probe: 07:00 and 08:46 with Fun 46 → `relax`; 07:00 with Fun 80 → no break; a child at 06:30 with Fun 40 → `relax`; 09:05 → `career_day`. `test_autonomy_policy` 167/0 and prework 40/40 stand.
- Age rules at start: `begin_current_action` cancels an age-gated action with its reason before any effect; the child jog and the post-birthday toys cases that failed in the iteration 56 probe now pass (queue emptied, "The treadmill is for teens and adults.", Fitness 0, hygiene unchanged).
- Emotion combination: the strongest moodlet and the activity's own feeling each count once (best bonus, one penalty), so a Happy or Unsettled reader is no longer worse off than a plain one; `_gain_skill` records base effort for chapter practice.
- Wall paint end to end: on the lot-0 starter home the 12.2 m north wall quotes §73 (matches the evidence frame), the first paint converts the legacy records to a canonical state carrying `material 8faf9f`, the rebuilt panel's albedo is `8faf9f`, a JSON snapshot reloads with the colour and the rebuilt panel, undo refunds and restores `eae7d7`; on a built room a new wall paints at §6/m (clay), other walls stay cream, a second coat replaces the first, undo peels the second then the first, the room itself still undoes afterwards, and a blush wall reloads blush. Refusals: same colour, wrong level, non-hex colour, unknown wall, open floor ("Point at a wall on this level."). `test_build_transactions` 47/0 includes the developer's four paint checks.
- Loveseat: `activity_resource_ids` yields `ls:left` / `ls:right` per slot and plain `ls` without one (older saves); a sofa ignores slots; `slot_approach` gives distinct points 1 m apart; `activity_anchor` seats at x ±0.45; a queued action's `seat_slot` survives JSON save and restore. `iteration57_loveseat_two_watchers.png` shows two Lifelets on the loveseat facing the set.
- Wall decor at every angle: all three wall-mounted kinds snap to placeable spots on north, south, west and east walls, in both corners, at the free end of an interior wall (x=1.13, z=0.475), and upstairs; the parallel-wall east case that failed in iteration 56 is accepted (`can_place` shifts by the unrotated depth; `wall_snap` clamps 0.3 m from the ends).
- Catalogue week run 5 (`evidence/catalogue_week_run5/`, 310 assertions, 2 failures): 547 completions; every purchased furnishing used; Morgan, Jamie, Avery, Ellis and Robin each rehearsed, played the piano or both; waiting per member 0.5–6.3 h (bed 5 sampled waits against 144 in run 4, fridge 34, bookshelf 22, shower 19); Fun minima 8.4–23.6; every adult 5 shifts, every pupil 5 school days with homework, 0 late minutes, four level-2 promotions.
- Harness: `test_busy_resources` observation loops at 900/1200 steps now cover the thirty-minute rule at one game minute per real second; the critic's rerun on the pinned export passes every phase with no source drift. GLB decode shows the re-authored `Hair_Long_Back` keeps its topology (280 vertices in the full models, 71→72 adult LOD, 71→73 child LOD) with the hood unchanged (392 / 91).

## Required next changes

**P1 — a late riser still leaves bored and comes home at Fun 10.** Run 5, Morgan Reed, day 4: asleep 03:14–08:46, Fun 45.9 at 08:46, `career_day` from 09:02, Fun 10.2 at 16:51, 8.4 at 17:20, 30.2 minutes under 10; the suite's "Fun never falls below 10" assertion is the retained failure. The mechanism is in `_autonomous_choice`: `include_fun` is `duty.is_empty()`, so once 09:00 passes the projection ignores Fun and "moderate boredom never delays leaving", although `LifeCareerSchedule.ON_TIME` is 10:00 and a 40-minute relax at 09:05 costs nothing. Probe: 09:05 with Fun 46 → `career_day`; the shift ends at Fun 18.3 with the softened −4 drain (the committed tweak lifts the floor, it does not restore the break). Player impact: a Lifelet who slept late spends the evening recovering instead of using the house. Acceptance: when the duty is due but `minutes + travel + candidate duration ≤ ON_TIME`, a Fun projection under 20 still takes the brief leisure; the prework suite gains a 09:05 case that relaxes and arrives on time; a rendered week on the final code (run 6) passes "Fun ≥ 10" for every member with the minima reported against run 5.

**P1 — the pastime rotation makes the pre-work break long enough to be late.** The pre-duty list is `relax, read, watch, paint, stretch, warm_up` and the same history penalty applies to it: with relax, read and watch as the last three pastimes (the normal end of an evening under the new rotation) the 08:30 break is a 90-minute canvas. Probe: 08:30 → paint, leaves 10:00, 0 late minutes (on the boundary); 08:40 → paint, leaves 10:10, `late_minutes 10.0` with reduced pay and performance. No rendered week shows it yet (runs 4 and 5 had no adult morning leisure at all), but the conditions are ordinary for a Creative adult who wakes after 08:30 with Fun under 48. Acceptance: the pre-duty branch only offers candidates whose duration fits before `ON_TIME − travel` (or ignores the history), stretch and warm-up sit ahead of paint, `test_prework_recovery` gains a history-laden 08:40 case with 0 late minutes, and run 6 reports late minutes per adult.

**P2.** Third watcher: `_resolve_activity_target` tests each seat with a trial action that carries no `seat_slot`, so a full loveseat (`ls:left`, `ls:right`) looks free to the trial, `_assign_seat_slot` then hands out `left` again and the real availability check fails; the Lifelet queues at the loveseat while a free armchair further from the set stands empty (by inspection of `main.gd:2855-2884`; unverified in play — add a three-watcher case). `construction.gd:_convert_proposal` turns a paint proposal into a wall removal when no `quote_provider` is bound (probe: 9 walls → 8); the shipped UI always binds one and `on_construction` refuses proposals without `build_quote`, so this is latent, but the legacy path should refuse `paint` outright. `_leisure_history` is not in `get_state`, so a reload restarts the rotation (probe: `[]` after restore). The suite's thresholds are looser than the acceptance asked for in iteration 56 (≥1 use per furnishing instead of ≥3; ≤6 h waiting instead of ≤4 h); say so in `tests/README.md` or tighten them. The Willow Cottage fixture has one bed, one fridge and one shower for eight Lifelets; the forty-five-minute rule now converts bed queues into evening naps and 03:00 sleeps (Morgan's day 3–4 shows nap 20:08, nap 23:00, sleep 03:14) — either give the fixture a second bed and fridge or make autonomy buy or prefer them, and report the sleep windows. The README's "bed or fridge busy for more than forty-five minutes" holds for the fridge only where a stove offers `cook`; a snack has no alternative target. Visual: the raised-sleeve shoulder tear in the stretch v2 crop; the long-hair slab; the flat mirror; one face per family; the swatch row and Skills panel at 960×600; no in-game frame of the Career "Next" line. Give the paint tool a room or both-faces option and a colour picker beyond five swatches. The frame-time gain between runs 2 and 5 should be attributed (LOD, autonomy churn or run-to-run noise) before it is claimed.

## Verification limits and scope remaining

No hands-on play, no game window, no packaged build. All frames are the developer's; the critic's contribution is crops, GLB decoding, code analysis, audit analysis of runs 3–5 and headless probes in an isolated project pinned to `aa32fbe`. The three catalogue weeks are the developer's runs of a harness that grants §6,000 and one trait; their audits were analysed, not reproduced (a rendered window is required), and no run exists for the final commit's Fun drain, Confident moodlet, emotion combination or V stretch. Probe checks supply arrival and are not routing or endurance evidence; the loveseat slot assignment, the nearest-free-seat search and the Career label live in `main.gd` and were verified by reading, not by running the scene. The wardrobe sheets are Blender renders, so the hair-over-hood claim is verified for source geometry only. The stretch close-ups are the developer's captures at an unknown cycle phase. Small-window layout, physical input, audio, save-picker portraits, home visits, upper-floor paint through the UI and the Blender export pipeline were not exercised.

**Decision: iterate.** The iteration 56 milestone is accepted with limitations: autonomy variety in a crowded home is proven in a rendered week; wall-decor placement at ±90° and in corners is verified; the Fun-across-a-shift repair is verified only in the preparation window. The iteration 57 milestone (paint, shared seating, queue rules) is accepted with limitations: paint is verified through propose, commit, undo, save and reload on legacy and canonical homes; the loveseat is verified at the world and save layers and in one frame; the final tuning commit is unverified in a rendered week. This is not a full-target claim.

## Three highest-value next iterations

1. **Close the crowded-week contract.** Take the brief leisure whenever arrival can still be on time (due or not), fit the pre-duty list to the departure deadline, save the pastime history, make the free-seat search slot-aware, then run a rendered week on the final code with the suite at the critic's thresholds (≥3 uses per furnishing, ≤4 h waiting, Fun ≥ 10, 0 late minutes) and a second bed and fridge in the fixture so the numbers describe autonomy rather than one bottleneck.
2. **Build depth on the paint foundation.** Room and both-faces paint, a wider colour choice, wallpaper or trim, per-room floor finishes, catalogue paging, and more architectural and outdoor pieces, each verified through purchase, save, reload and travel; refuse `paint` in the legacy proposal path.
3. **Characters in the renderer.** Re-weight the hoodie and jacket shoulders so the raised sleeve holds, drape the long hair instead of boxing it, age-specific faces, a mirror that reflects or reads as glass, production close-ups per age for the new pieces, and captures at 960×600 with a profiled frame budget for eight Lifelets.
