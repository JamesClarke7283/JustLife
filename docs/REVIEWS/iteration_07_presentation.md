# Independent follow-up — school contact and birthday presentation

8 September 2026. **The final desk correction is accepted after a further 39 clean rendered checks and direct side-view inspection. Full-request score remains 7.2/10.** The earlier 94-check run below passed its numeric assertions but still exposed arm penetration; the final correction is documented at the end.

The frozen source is `/tmp/justlife-school-presentation-source-fo1yojao`, containing the first visible booster, forward child seat position and adaptive torso lean. The corrected critic harness `tests/test_school_presentation.gd` ran in `/tmp/justlife-playthrough-hzu7j5cy` through the public creator, actual homework/class processing and birthday. It did not repeat named-save verification. Evidence is preserved in `art/screenshots/school_presentation_iteration_07/`.

An initial harness run used obstructed inspection cameras and incorrectly accepted the arrival frame's previous walking-animation timer as the birthday timer. Its two prop-visibility failures were harness timing errors, not production birthday defects. The corrected predicate waits for the actor's actual birthday animation before checking its time. Production source remained unchanged between these two runs.

## Accepted corrections

- The child begins with the visible, attainable “Get a snack from the fridge” want. The unavailable first-cooking wish is absent. This check verifies the displayed objective; the parent's separate reward/migration tests are not counted in these 94 checks.
- The completed school record now correctly says one class and one assignment. Homework, class learning, preparation consumption, pause and need/fund effects still pass in this flow.
- The birthday's actual normal-speed phases are now visible in close inspection: at 2.57 seconds the cake has three lit candles; at 3.42 seconds the cake remains and all flames are out; at 4.81 seconds the cake is gone and the hands perform applause. Actual completion still advances the child to teen. The cake has visible hand support. This is a meaningful improvement over the earlier idle pose.

## Desk defect still open

The initial reach failure is corrected numerically. Real child palm positions now land within a few millimeters of the world keyboard targets, and a mint booster is visibly attached to the chair. However, the unoccluded side and over-shoulder captures show both forearms passing through the tabletop/laptop edge. Only parts of the hands emerge above the keyboard.

The supported hip point is now 0.79m high, with the keyboard target at 1.075m. The child leans forward to achieve reach, leaving shoulders too low for a clear forearm path. The next correction must make the shoulder, elbow and forearm clear the actual desktop and keyboard geometry, while preserving believable support on the chair. Extending the arms or passing palm-target tests alone is insufficient.

The birthday remains a solitary event at the fridge; group participation, serving/eating cake and a richer occasion are still broader interaction opportunities. Grip morph work is staged separately and is not credited in this frozen build.


## Final desk correction accepted

The next source, `/tmp/justlife-desk-source-ign6rf9v`, combines a visible 0.18m booster with upward/outward elbow placement and whole-arm clearance checks against the actual desk surface and front edge. Critic-owned `tests/test_desk_clearance.gd`, run via `--suite desk` in `/tmp/justlife-playthrough-stkdpm_m`, passed **39 checks with zero runtime errors**. Evidence and source/model hashes are preserved in `art/screenshots/desk_clearance_iteration_07/`.

The child again reaches the class through actual public controls and routed movement. Clear side and over-shoulder screenshots now show continuous forearms above the tabletop and laptop edge, with hands contacting the keyboard. The booster is visible and supports the forward sitting position. Actual shoulder height is approximately 1.107m and elbows approximately 1.136m, above the 1.03m desktop; palm errors remain about 1.1–4.1mm. The posture is a little extended, but credible for a child using this desk. Canceling the inspected partial class leaves attendance at zero.

This resolves the specific default-child desk clearance defect. The independent visual sample is the normal creator scale at Willow Cottage; the developer's wider scale/yaw tests remain supporting checks, not additional visual samples claimed by the critic. The school progression/save results were not needlessly repeated, and the birthday presentation remains accepted from the previous frozen run.
